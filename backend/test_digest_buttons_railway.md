# Инструкция по проверке работы кнопок Daily/Weekly Digest

## Способ 1: Проверка через Telegram бота (Рекомендуется)

### Шаги:

1. **Откройте Telegram бота** и отправьте команду `/start`
   - Должно появиться меню с кнопками:
     - 📅 Daily Digest
     - 📊 Weekly Digest
     - ⚙️ Digest Settings
     - 📚 Help

2. **Нажмите кнопку "📅 Daily Digest"**
   - Ожидаемое поведение:
     - Сразу придет сообщение: "📅 Дневной дайджест (всех новостей/только отслеживаемых компаний) генерируется..."
     - Через несколько секунд придет сам дайджест с новостями

3. **Нажмите кнопку "📊 Weekly Digest"**
   - Ожидаемое поведение:
     - Сразу придет сообщение: "📊 Недельный дайджест (всех новостей/только отслеживаемых компаний) генерируется..."
     - Через несколько секунд придет сам дайджест с новостями

### Если кнопки не работают:

1. Проверьте, что webhook установлен правильно:
   ```bash
   curl "https://web-production-6bf5.up.railway.app/api/v1/telegram/get-webhook-info"
   ```

2. Проверьте, что пользователь найден в БД:
   ```sql
   SELECT user_id, telegram_chat_id, telegram_enabled, digest_enabled
   FROM user_preferences
   WHERE telegram_chat_id = 'ВАШ_CHAT_ID';
   ```

3. Убедитесь, что `telegram_enabled = true`

## Способ 2: Мониторинг через логи Railway

### Проверка логов Web сервиса:

```bash
railway logs --service "web" --since 10m
```

**Что искать:**
- `Processing callback from <chat_id>: digest_daily` или `digest_weekly`
- `Starting digest generation for user: <user_id>`
- `Digest sent to Telegram for user <user_id>`

### Проверка логов Worker сервиса:

```bash
railway logs --service "worker" --since 10m
```

**Что искать:**
- `[INFO/ForkPoolWorker-X] Task app.tasks.digest.generate_user_digest[<task_id>] received`
- `Digest generation completed for user: <user_id>`

### Проверка логов Beat сервиса (для автоматических дайджестов):

```bash
railway logs --service "beat" --since 10m
```

## Способ 3: Проверка через API endpoint

Можно также протестировать через прямой вызов API (но это требует авторизации):

```bash
# Получить информацию о webhook
curl "https://web-production-6bf5.up.railway.app/api/v1/telegram/get-webhook-info"
```

## Типичные проблемы и решения:

### Проблема: Кнопка нажата, но ничего не происходит

**Решение:**
1. Проверьте логи web сервиса на наличие ошибок
2. Убедитесь, что worker запущен и обрабатывает задачи
3. Проверьте подключение к Redis (Celery использует Redis для очередей)

### Проблема: Сообщение "Генерируется..." приходит, но дайджест не приходит

**Решение:**
1. Проверьте логи worker - возможно задача упала с ошибкой
2. Проверьте, что в БД есть новости за последние 24 часа (для Daily) или 7 дней (для Weekly)
3. Проверьте, что `telegram_chat_id` правильный и `telegram_enabled = true`

### Проблема: Ошибка "User not found or Telegram not configured"

**Решение:**
1. Убедитесь, что Chat ID добавлен в настройки профиля
2. Проверьте в БД: `telegram_chat_id` должен совпадать с вашим Chat ID
3. Убедитесь, что `telegram_enabled = true` в БД

## Быстрая проверка статуса:

```bash
# Проверить статус всех сервисов Railway
railway status

# Проверить переменные окружения
railway variables

# Проверить метрики использования
railway metrics
```

















