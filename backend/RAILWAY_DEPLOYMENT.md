# Railway Deployment Guide

## 🚀 Быстрый деплой на Railway

### 1. Подготовка переменных окружения

В Railway Dashboard добавьте следующие переменные:

#### Обязательные переменные:
```
SECRET_KEY=your-super-secret-key-here-change-this
ENVIRONMENT=production
DEBUG=false
```

#### База данных и Redis:
Railway автоматически предоставит:
- `DATABASE_URL` (PostgreSQL)
- `REDIS_URL` (Redis)

#### Celery (опционально):
```
CELERY_BROKER_URL=redis://default:**@redis.railway.internal:6379/0
CELERY_RESULT_BACKEND=redis://default:**@redis.railway.internal:6379/0
```

### 2. Деплой

1. Подключите репозиторий к Railway
2. Railway автоматически использует `railway.json` конфигурацию
3. Используется `Dockerfile.railway` для сборки
4. Приложение запускается через `python main.py`

### 3. Проверка

После деплоя проверьте:
- Логи на предмет ошибок
- Health check: `https://your-app.railway.app/health`
- API docs: `https://your-app.railway.app/docs`

### 4. Troubleshooting

#### Ошибка "Missing required environment variables":
- Проверьте, что все обязательные переменные установлены в Railway Dashboard

#### Ошибка "Invalid PORT value":
- Railway автоматически устанавливает PORT, проблема должна быть решена

#### Ошибка подключения к базе данных:
- Убедитесь, что PostgreSQL сервис добавлен в Railway проект
- Проверьте, что `DATABASE_URL` установлена автоматически

#### Ошибка подключения к Redis:
- Убедитесь, что Redis сервис добавлен в Railway проект
- Проверьте, что `REDIS_URL` установлена автоматически

### 5. Дополнительные сервисы

Для полной функциональности добавьте:
- **PostgreSQL** - для основной базы данных
- **Redis** - для кэша и очередей
- **Worker** - для фоновых задач (Celery)
- **Beat** - для планировщика задач

### 6. Мониторинг

- Логи доступны в Railway Dashboard
- Health check endpoint: `/health`
- Метрики в Railway Dashboard
