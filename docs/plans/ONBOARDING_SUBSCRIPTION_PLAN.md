# План работ: Онбординг и система подписок

## 🎯 Приоритеты реализации

**Фаза 1: Основной функционал (онбординг)**
- Реализация 5-экранного онбординг флоу
- Интеграция GPT для генерации AI-описаний
- Сохранение состояния онбординга
- Регистрация в середине флоу
- Создание наблюдения за конкурентами

**Фаза 2: Монетизация (подписки)**
- Система подписок и триалов
- Интеграция с платежными системами
- Проверка доступа к функциям

---

## 📋 Анализ текущей системы

### Что уже реализовано:

1. **Аутентификация и регистрация:**
   - ✅ Backend: `backend/app/api/v1/endpoints/auth.py` - регистрация и логин
   - ✅ Frontend: `frontend/src/pages/RegisterPage.tsx`, `LoginPage.tsx`
   - ✅ JWT токены, refresh tokens
   - ✅ Модель User в `backend/app/models/user.py`

2. **Работа с компаниями:**
   - ✅ API `/api/v1/companies/scan` - сканирование сайта компании
   - ✅ API `/api/v1/companies/` - создание/обновление компании
   - ✅ API `/api/v1/competitors/suggest/{company_id}` - предложение конкурентов
   - ✅ Frontend: `CompanySearchModal`, `AddCompetitorModal` - модальные окна для добавления
   - ✅ Сервис `CompanyInfoExtractor` - извлечение метаданных компании

3. **Система уведомлений:**
   - ✅ Модели `NotificationChannel`, `NotificationSubscription` (но это про уведомления, не платежи)
   - ✅ Система подписок на уведомления

4. **Настройки OpenAI:**
   - ✅ В `backend/app/core/config.py` есть `OPENAI_API_KEY` и `OPENAI_MODEL`
   - ⚠️ API key не интегрирован в систему (нужно добавить)
   - ⚠️ Используется `HeuristicNLPProvider` вместо GPT для классификации
   - ✅ Есть `OpenAIScraper` для скрапинга блогов OpenAI (не для генерации описаний)

### Что отсутствует:

1. ❌ **Система подписок и платежей:**
   - Нет модели Subscription
   - Нет модели Trial
   - Нет интеграции с платежными системами
   - Нет проверки статуса подписки при доступе к функциям

2. ❌ **Онбординг флоу:**
   - Есть простой модальный попап в `DashboardPageTest`, но нет полноценного 5-экранного флоу
   - Нет сохранения состояния онбординга
   - Нет интеграции регистрации в середине флоу

3. ❌ **Управление триалом:**
   - Нет логики автоматического запуска триала после онбординга
   - Нет проверки истечения триала
   - Нет уведомлений об окончании триала

---

## 🎯 Новые требования

### 1. Онбординг флоу (5 экранов)

**Экран 1 — Ввод материнской компании**
- Поле ввода URL сайта
- Placeholder: `https://example.com`
- Кнопка "Анализировать"
- Backend: быстрый анализ сайта (парсинг title/meta, извлечение описания, категории, бренда, ключевых слов, соцсети)
- Создание объекта session/project: `{company_url, company_name, industry}`

**Экран 2 — Карточка материнской компании**
- Название: "Мы нашли вашу компанию"
- Отображение: название бренда, лого (если найдено), AI-описание (2-3 строки), категория/индустрия, основные сигналы
- Кнопка "Далее: выбрать конкурентов"

**Экран 3 — Выбор конкурентов**
- Заголовок: "Выберите конкурентов для мониторинга"
- Блок из 10 карточек конкурентов
- У каждой карточки: название, лого, описание, сайт, чекбокс "следить", крестик "заменить"
- Логика: генерация первых 10 конкурентов на основе индустрии, ключевых слов, похожих сайтов
- При нажатии крестика — карточка заменяется новой (бесконечная лента)
- Можно выбрать от 1 до 50+ конкурентов
- Кнопки: "Продолжить" (активна при ≥1 выборе), "Показать ещё 10"
- **После этого экрана — регистрация** (если пользователь не авторизован)

**Экран 4 — Создание наблюдения**
- Название: "Настраиваем наблюдение за конкурентами"
- Backend: запуск процессов (поиск соцсетей, парсинг сайтов, сбор новостей, отслеживание маркетинга, SEO/SEM сигналы)
- UI: крутилка + текст "Мы собираем данные о выбранных конкурентах. Это займёт 15–60 секунд"
- После загрузки → кнопка "Перейти к дайджесту"

**Экран 5 — Подтверждение подписки**
- Заголовок: "Ваш наблюдательный центр готов"
- Показаны выбранные конкуренты (лого + название)
- Текст: "Каждый день вы будете получать дайджест изменений по вашим конкурентам: новости, маркетинг, продуктовые обновления и многое другое"
- Кнопка "Начать"

### 2. Система подписок

- **Одна подписка на $29/месяц**
- **Триал на 3 дня** после онбординга
- После окончания триала — автоматическое предложение подписки

### 3. Регистрация

- **Выскакивает после Экрана 3** (выбор конкурентов)
- Если пользователь уже авторизован — пропускается
- После регистрации — продолжение онбординга с сохранением выбранных данных

---

## 🏗️ План реализации

---

## ФАЗА 1: ОСНОВНОЙ ФУНКЦИОНАЛ (ОНБОРДИНГ)

### Этап 1.1: Backend — Интеграция GPT для генерации описаний

#### 1.1.1. Сервис GPT для онбординга

**Файл:** `backend/app/services/gpt_service.py`

**Методы:**

1. `generate_company_description(company_data: dict) -> str`
   - Генерирует AI-описание компании (2-3 строки)
   - Использует данные: название, сайт, мета-теги, категория
   - Промпт: "Создай краткое описание компании (2-3 предложения) на основе данных: {company_data}"

2. `generate_competitor_description(competitor_data: dict, parent_company: str) -> str`
   - Генерирует описание конкурента
   - Учитывает связь с материнской компанией
   - Промпт: "Опиши компанию {competitor_name} как конкурента для {parent_company}. Кратко (1-2 предложения)"

3. `suggest_industry_signals(company_data: dict) -> List[str]`
   - Предлагает основные сигналы/характеристики компании
   - Например: "FMCG", "ритейл", "ecommerce", "SaaS"
   - Промпт: "Определи основные характеристики компании: {company_data}. Верни список из 3-5 ключевых слов"

**Задачи:**
- [x] Создать файл `gpt_service.py` ✅
- [x] Интегрировать OpenAI API (использовать `OPENAI_API_KEY` из config) ✅
- [x] Реализовать методы генерации описаний ✅
- [x] Добавить обработку ошибок и fallback на эвристические описания ✅
- [ ] Добавить кэширование результатов (опционально)

#### 1.1.2. Обновление конфигурации

**Файл:** `backend/app/core/config.py`

**Проверка:**
- [ ] Убедиться, что `OPENAI_API_KEY` правильно загружается из env
- [ ] Добавить валидацию наличия ключа (если нужно)
- [ ] Добавить настройки для промптов (опционально)

**Задачи:**
- [ ] Проверить загрузку `OPENAI_API_KEY`
- [ ] Добавить в `env.example` пример настройки

---

### Этап 1.2: Backend — Модель онбординга

#### 1.2.1. Модель OnboardingSession

**Файл:** `backend/app/models/onboarding.py`

```python
class OnboardingStep(str, Enum):
    COMPANY_INPUT = "company_input"
    COMPANY_CARD = "company_card"
    COMPETITOR_SELECTION = "competitor_selection"
    OBSERVATION_SETUP = "observation_setup"
    SUBSCRIPTION_CONFIRM = "subscription_confirm"
    COMPLETED = "completed"

class OnboardingSession(BaseModel):
    __tablename__ = "onboarding_sessions"
    
    id: UUID (primary key)
    user_id: Optional[UUID]  # Может быть null до регистрации
    session_token: str  # Уникальный токен для анонимных сессий
    
    # Данные онбординга
    current_step: OnboardingStep
    company_data: JSON  # Данные материнской компании
    selected_competitors: JSON  # Список выбранных конкурентов
    observation_config: JSON  # Конфигурация наблюдения
    
    # Метаданные
    created_at: datetime
    updated_at: datetime
    completed_at: Optional[datetime]
```

**Задачи:**
- [ ] Создать модель `OnboardingSession`
- [ ] Создать enum `OnboardingStep`
- [ ] Добавить индексы для быстрого поиска по `session_token` и `user_id`
- [ ] Создать миграцию Alembic

---

### Этап 1.3: Backend — API онбординга

**Файл:** `backend/app/api/v1/endpoints/onboarding.py`

**Эндпоинты:**

1. **POST `/api/v1/onboarding/start`**
   - Создает новую сессию онбординга
   - Возвращает `session_token`
   - Если пользователь авторизован — привязывает к `user_id`

2. **POST `/api/v1/onboarding/company/analyze`**
   - Принимает `website_url` и `session_token`
   - Выполняет быстрый анализ сайта (использует существующий `extract_company_info`)
   - **Генерирует AI-описание через GPT** (использует новый `GPTService`)
   - Сохраняет данные в `onboarding_session.company_data`
   - Возвращает данные компании для Экрана 2

3. **GET `/api/v1/onboarding/company`**
   - Получает данные компании из сессии по `session_token`
   - Для Экрана 2

4. **GET `/api/v1/onboarding/competitors/suggest`**
   - Генерирует 10 конкурентов на основе данных компании из сессии
   - Использует существующий `CompetitorAnalysisService.suggest_competitors()`
   - **Генерирует AI-описания для каждого конкурента через GPT**
   - Возвращает список конкурентов с возможностью замены

5. **POST `/api/v1/onboarding/competitors/replace`**
   - Заменяет одного конкурента на нового
   - Принимает `competitor_id_to_replace` и `session_token`
   - **Генерирует AI-описание для нового конкурента**
   - Возвращает нового конкурента

6. **POST `/api/v1/onboarding/competitors/select`**
   - Сохраняет выбранных конкурентов в сессию
   - Принимает `selected_competitor_ids[]` и `session_token`
   - Возвращает подтверждение

7. **POST `/api/v1/onboarding/observation/setup`**
   - Запускает процессы наблюдения (Celery задачи)
   - Принимает `session_token`
   - Возвращает `task_id` для отслеживания прогресса
   - Задачи:
     - Поиск соцсетей конкурентов
     - Парсинг сайтов
     - Сбор новостей
     - Отслеживание маркетинга
     - SEO/SEM сигналы

8. **GET `/api/v1/onboarding/observation/status`**
   - Проверяет статус задач наблюдения
   - Принимает `task_id`
   - Возвращает `status: "pending" | "processing" | "completed"` и прогресс

9. **POST `/api/v1/onboarding/complete`**
   - Завершает онбординг
   - Принимает `session_token` и `user_id` (если регистрация была после экрана 3)
   - Создает компании и подписки на конкурентов
   - **Пока БЕЗ создания подписки/триала** (это будет в Фазе 2)
   - Возвращает данные для Экрана 5

**Задачи:**
- [x] Создать файл `onboarding.py` ✅
- [x] Реализовать все эндпоинты (9 эндпоинтов) ✅
- [x] Интегрировать `GPTService` для генерации описаний ✅
- [x] Добавить валидацию и обработку ошибок ✅
- [x] Интегрировать с существующими сервисами ✅

---

### Этап 1.4: Frontend — Компоненты онбординга

#### 1.4.1. Страница онбординга

**Файл:** `frontend/src/pages/OnboardingPage.tsx`

**Структура:**
- Управление состоянием текущего экрана
- Сохранение данных в localStorage (для анонимных пользователей)
- Интеграция с API онбординга
- Навигация между экранами

**Задачи:**
- [ ] Создать страницу `OnboardingPage.tsx`
- [ ] Добавить роут `/onboarding` в `App.tsx`
- [ ] Реализовать логику переключения экранов

#### 1.4.2. Экран 1: Ввод компании

**Файл:** `frontend/src/components/onboarding/CompanyInputStep.tsx`

**Компонент:**
- Поле ввода URL
- Placeholder: `https://example.com`
- Кнопка "Анализировать"
- Индикатор загрузки
- Обработка ошибок

**Задачи:**
- [ ] Создать компонент
- [ ] Интегрировать с API `/onboarding/company/analyze`
- [ ] Валидация URL
- [ ] Переход на Экран 2 после успешного анализа

#### 1.4.3. Экран 2: Карточка компании

**Файл:** `frontend/src/components/onboarding/CompanyCardStep.tsx`

**Компонент:**
- Отображение названия бренда
- Логотип (если есть)
- **AI-описание (2-3 строки)** - генерируется через GPT
- Категория/индустрия
- Основные сигналы (генерируются через GPT)
- Кнопка "Далее: выбрать конкурентов"

**Задачи:**
- [x] Создать компонент ✅ (`CompanyCardStep.tsx`)
- [x] Получение данных из API `/onboarding/company` ✅
- [x] Красивый UI с карточкой компании ✅
- [x] Отображение AI-описания ✅
- [x] Переход на Экран 3 ✅

#### 1.4.4. Экран 3: Выбор конкурентов

**Файл:** `frontend/src/components/onboarding/CompetitorSelectionStep.tsx`

**Компонент:**
- Заголовок "Выберите конкурентов для мониторинга"
- Сетка из 10 карточек конкурентов
- У каждой карточки:
  - Название компании
  - Логотип
  - **AI-описание (генерируется через GPT)**
  - Сайт
  - Чекбокс "следить"
  - Крестик "заменить"
- Кнопка "Показать ещё 10" (загружает следующую порцию)
- Кнопка "Продолжить" (активна при ≥1 выборе)
- **После нажатия "Продолжить" — проверка авторизации:**
  - Если не авторизован → показывается модальное окно регистрации
  - После регистрации → продолжение онбординга

**Задачи:**
- [x] Создать компонент ✅ (`CompetitorSelectionStep.tsx`)
- [x] Интегрировать с API `/onboarding/competitors/suggest` ✅
- [x] Реализовать замену конкурентов через API `/onboarding/competitors/replace` ✅
- [x] Сохранение выбранных через API `/onboarding/competitors/select` ✅
- [x] Интеграция с модальным окном регистрации ✅
- [x] Отображение AI-описаний конкурентов ✅
- [x] Переход на Экран 4 после регистрации/авторизации ✅

#### 1.4.5. Экран 4: Создание наблюдения

**Файл:** `frontend/src/components/onboarding/ObservationSetupStep.tsx`

**Компонент:**
- Заголовок "Настраиваем наблюдение за конкурентами"
- Индикатор загрузки (крутилка)
- Текст "Мы собираем данные о выбранных конкурентах. Это займёт 15–60 секунд"
- Polling статуса через API `/onboarding/observation/status`
- После завершения → кнопка "Перейти к дайджесту"

**Задачи:**
- [ ] Создать компонент
- [ ] Интегрировать с API `/onboarding/observation/setup`
- [ ] Реализовать polling статуса
- [ ] Отображение прогресса (если API возвращает)
- [ ] Переход на Экран 5 после завершения

#### 1.4.6. Экран 5: Подтверждение

**Файл:** `frontend/src/components/onboarding/OnboardingCompleteStep.tsx`

**Компонент:**
- Заголовок "Ваш наблюдательный центр готов"
- Список выбранных конкурентов (лого + название)
- Текст о ежедневных дайджестах
- Кнопка "Начать"
- После нажатия → завершение онбординга через API `/onboarding/complete`
- Редирект на `/dashboard` или `/dashboard-test`

**Задачи:**
- [ ] Создать компонент
- [ ] Интегрировать с API `/onboarding/complete`
- [ ] Отображение выбранных конкурентов
- [ ] Редирект после завершения

#### 1.4.7. Модальное окно регистрации в онбординге

**Файл:** `frontend/src/components/onboarding/OnboardingRegisterModal.tsx`

**Компонент:**
- Модальное окно с формой регистрации
- Поля: email, пароль, подтверждение пароля, имя
- Валидация
- После успешной регистрации:
  - Автоматический логин
  - Привязка `onboarding_session` к `user_id`
  - Продолжение онбординга

**Задачи:**
- [ ] Создать компонент
- [ ] Интегрировать с API регистрации
- [ ] Обработка ошибок
- [ ] Сохранение состояния онбординга при регистрации

---

### Этап 1.5: Обновление API сервиса (Frontend)

**Файл:** `frontend/src/services/api.ts`

**Добавить методы:**

1. `startOnboarding() -> { session_token: string }`
2. `analyzeCompanyForOnboarding(websiteUrl: string, sessionToken: string) -> CompanyData`
3. `getOnboardingCompany(sessionToken: string) -> CompanyData`
4. `suggestCompetitorsForOnboarding(sessionToken: string) -> Competitor[]`
5. `replaceCompetitorInOnboarding(competitorId: string, sessionToken: string) -> Competitor`
6. `selectCompetitorsInOnboarding(competitorIds: string[], sessionToken: string) -> void`
7. `setupObservation(sessionToken: string) -> { task_id: string }`
8. `getObservationStatus(taskId: string) -> { status: string, progress?: number }`
9. `completeOnboarding(sessionToken: string, userId?: string) -> { success: boolean }`

**Задачи:**
- [ ] Добавить методы в `ApiService`
- [ ] Типизация TypeScript
- [ ] Обработка ошибок

---

## ФАЗА 2: МОНЕТИЗАЦИЯ (ПОДПИСКИ)

> ⚠️ **Эта фаза реализуется ПОСЛЕ завершения Фазы 1**

### Фаза 2.1: Backend — Модели и миграции подписок

#### 2.1.1. Модель Subscription

**Файл:** `backend/app/models/subscription.py`

```python
class SubscriptionStatus(str, Enum):
    TRIAL = "trial"
    ACTIVE = "active"
    CANCELLED = "cancelled"
    EXPIRED = "expired"

class Subscription(BaseModel):
    __tablename__ = "subscriptions"
    
    user_id: UUID
    status: SubscriptionStatus
    plan_type: str = "monthly"  # Пока только monthly
    price: Decimal = Decimal("29.00")
    currency: str = "USD"
    
    # Даты
    trial_started_at: Optional[datetime]
    trial_ends_at: Optional[datetime]
    started_at: Optional[datetime]
    expires_at: Optional[datetime]
    cancelled_at: Optional[datetime]
    
    # Платежи
    payment_provider: Optional[str]  # stripe, paypal, etc.
    payment_subscription_id: Optional[str]  # ID в платежной системе
    
    # Метаданные
    metadata: JSON = {}
```

**Задачи:**
- [ ] Создать модель `Subscription`
- [ ] Создать enum `SubscriptionStatus`
- [ ] Добавить relationship к User
- [ ] Создать миграцию Alembic

#### 2.1.2. Обновление модели User

**Файл:** `backend/app/models/user.py`

**Изменения:**
- [ ] Добавить relationship к Subscription (one-to-one)
- [ ] Добавить метод `has_active_subscription()` для проверки доступа

**Задачи:**
- [ ] Обновить модель User
- [ ] Создать миграцию (если нужно)

---

### Фаза 2.2: Backend — Сервис подписок

**Файл:** `backend/app/services/subscription_service.py`

**Методы:**

1. `create_trial_subscription(user_id: UUID) -> Subscription`
   - Создает подписку с триалом на 3 дня
   - Устанавливает `trial_started_at`, `trial_ends_at`
   - Статус `TRIAL`

2. `check_subscription_access(user_id: UUID) -> bool`
   - Проверяет, есть ли активная подписка или триал
   - Учитывает `trial_ends_at` и `expires_at`

3. `activate_subscription(subscription_id: UUID, payment_data: dict) -> Subscription`
   - Активирует подписку после оплаты
   - Обновляет статус на `ACTIVE`
   - Устанавливает `started_at`, `expires_at` (+30 дней)

4. `cancel_subscription(subscription_id: UUID) -> Subscription`
   - Отменяет подписку
   - Устанавливает `cancelled_at`

5. `get_user_subscription(user_id: UUID) -> Optional[Subscription]`
   - Получает текущую подписку пользователя

**Задачи:**
- [ ] Создать `SubscriptionService`
- [ ] Реализовать все методы
- [ ] Добавить Celery задачу для проверки истечения триалов/подписок

---

### Фаза 2.3: Backend — API подписок

**Файл:** `backend/app/api/v1/endpoints/subscriptions.py`

**Эндпоинты:**

1. **GET `/api/v1/subscriptions/current`**
   - Получает текущую подписку пользователя
   - Возвращает статус, даты триала, даты подписки

2. **POST `/api/v1/subscriptions/create`**
   - Создает подписку (после оплаты)
   - Принимает `payment_provider`, `payment_subscription_id`
   - Обновляет статус Subscription на `ACTIVE`

3. **POST `/api/v1/subscriptions/cancel`**
   - Отменяет подписку
   - Устанавливает `cancelled_at` и статус `CANCELLED`

4. **GET `/api/v1/subscriptions/check-access`**
   - Проверяет доступ пользователя к функциям
   - Возвращает `has_access: bool`, `reason: str` (если нет доступа)

**Задачи:**
- [ ] Создать файл `subscriptions.py`
- [ ] Реализовать эндпоинты
- [ ] Интегрировать с `SubscriptionService`
- [ ] Добавить middleware для проверки подписки (опционально)

---

### Фаза 2.4: Backend — Обновление онбординга для создания триала

**Файл:** `backend/app/api/v1/endpoints/onboarding.py`

**Изменения:**
- [ ] В эндпоинте `POST /onboarding/complete` добавить создание триала
- [ ] Вызывать `SubscriptionService.create_trial_subscription()` после завершения онбординга

**Задачи:**
- [ ] Обновить эндпоинт завершения онбординга
- [ ] Интегрировать с `SubscriptionService`

---

### Фаза 2.5: Backend — Celery задачи

**Файл:** `backend/app/tasks/subscriptions.py`

**Задачи:**

1. `check_expired_trials()`
   - Периодическая задача (каждый час)
   - Находит триалы, у которых `trial_ends_at < now()`
   - Обновляет статус на `EXPIRED`
   - Отправляет уведомление пользователю

2. `check_expired_subscriptions()`
   - Периодическая задача (каждый час)
   - Находит подписки, у которых `expires_at < now()`
   - Обновляет статус на `EXPIRED`

**Задачи:**
- [ ] Создать файл `subscriptions.py` в `app/tasks/`
- [ ] Реализовать задачи
- [ ] Добавить в Celery Beat расписание

---

### Фаза 2.6: Frontend — Интеграция подписок

#### 2.6.1. Компонент статуса подписки

**Файл:** `frontend/src/components/subscription/SubscriptionStatus.tsx`

**Компонент:**
- Отображение текущего статуса подписки
- Информация о триале (сколько дней осталось)
- Кнопка "Оформить подписку" (если триал истекает)
- Интеграция с платежной системой (будущее)

**Задачи:**
- [ ] Создать компонент
- [ ] Интегрировать с API `/subscriptions/current`
- [ ] Отображение дней до окончания триала
- [ ] Добавить в Header или Dashboard

#### 2.6.2. Проверка доступа к функциям

**Файл:** `frontend/src/hooks/useSubscriptionAccess.ts`

**Хук:**
- Проверяет доступ пользователя к функциям
- Использует API `/subscriptions/check-access`
- Возвращает `hasAccess: boolean`, `reason: string`
- Кэширование результата

**Задачи:**
- [ ] Создать хук
- [ ] Интегрировать в защищенные компоненты
- [ ] Показывать баннер о необходимости подписки (если нет доступа)

#### 2.6.3. Обновление API сервиса

**Файл:** `frontend/src/services/api.ts`

**Добавить методы:**

1. `getCurrentSubscription() -> Subscription`
2. `checkSubscriptionAccess() -> { has_access: boolean, reason?: string }`
3. `createSubscription(paymentData: PaymentData) -> Subscription`
4. `cancelSubscription() -> void`

**Задачи:**
- [ ] Добавить методы в `ApiService`
- [ ] Типизация TypeScript
- [ ] Обработка ошибок

---

## 🔄 Последовательность разработки (обновленная)

#### 1.1. Модель Subscription

**Файл:** `backend/app/models/subscription.py`

```python
class SubscriptionStatus(str, Enum):
    TRIAL = "trial"
    ACTIVE = "active"
    CANCELLED = "cancelled"
    EXPIRED = "expired"

class Subscription(BaseModel):
    __tablename__ = "subscriptions"
    
    user_id: UUID
    status: SubscriptionStatus
    plan_type: str = "monthly"  # Пока только monthly
    price: Decimal = Decimal("29.00")
    currency: str = "USD"
    
    # Даты
    trial_started_at: Optional[datetime]
    trial_ends_at: Optional[datetime]
    started_at: Optional[datetime]
    expires_at: Optional[datetime]
    cancelled_at: Optional[datetime]
    
    # Платежи
    payment_provider: Optional[str]  # stripe, paypal, etc.
    payment_subscription_id: Optional[str]  # ID в платежной системе
    
    # Метаданные
    metadata: JSON = {}
```

**Задачи:**
- [ ] Создать модель `Subscription`
- [ ] Создать enum `SubscriptionStatus`
- [ ] Добавить relationship к User
- [ ] Создать миграцию Alembic

#### 1.2. Модель OnboardingSession

**Файл:** `backend/app/models/onboarding.py`

```python
class OnboardingStep(str, Enum):
    COMPANY_INPUT = "company_input"
    COMPANY_CARD = "company_card"
    COMPETITOR_SELECTION = "competitor_selection"
    OBSERVATION_SETUP = "observation_setup"
    SUBSCRIPTION_CONFIRM = "subscription_confirm"
    COMPLETED = "completed"

class OnboardingSession(BaseModel):
    __tablename__ = "onboarding_sessions"
    
    id: UUID (primary key)
    user_id: Optional[UUID]  # Может быть null до регистрации
    session_token: str  # Уникальный токен для анонимных сессий
    
    # Данные онбординга
    current_step: OnboardingStep
    company_data: JSON  # Данные материнской компании
    selected_competitors: JSON  # Список выбранных конкурентов
    observation_config: JSON  # Конфигурация наблюдения
    
    # Метаданные
    created_at: datetime
    updated_at: datetime
    completed_at: Optional[datetime]
```

**Задачи:**
- [ ] Создать модель `OnboardingSession`
- [ ] Создать enum `OnboardingStep`
- [ ] Добавить индексы для быстрого поиска по `session_token` и `user_id`
- [ ] Создать миграцию Alembic

#### 1.3. Обновление модели User

**Файл:** `backend/app/models/user.py`

**Изменения:**
- [ ] Добавить relationship к Subscription (one-to-one)
- [ ] Добавить relationship к OnboardingSession (one-to-many)
- [ ] Добавить метод `has_active_subscription()` для проверки доступа

**Задачи:**
- [ ] Обновить модель User
- [ ] Создать миграцию (если нужно)

---

### Фаза 2: Backend — API эндпоинты

#### 2.1. API онбординга

**Файл:** `backend/app/api/v1/endpoints/onboarding.py`

**Эндпоинты:**

1. **POST `/api/v1/onboarding/start`**
   - Создает новую сессию онбординга
   - Возвращает `session_token`
   - Если пользователь авторизован — привязывает к `user_id`

2. **POST `/api/v1/onboarding/company/analyze`**
   - Принимает `website_url` и `session_token`
   - Выполняет быстрый анализ сайта (использует существующий `extract_company_info`)
   - Сохраняет данные в `onboarding_session.company_data`
   - Возвращает данные компании для Экрана 2

3. **GET `/api/v1/onboarding/company`**
   - Получает данные компании из сессии по `session_token`
   - Для Экрана 2

4. **GET `/api/v1/onboarding/competitors/suggest`**
   - Генерирует 10 конкурентов на основе данных компании из сессии
   - Использует существующий `CompetitorAnalysisService.suggest_competitors()`
   - Возвращает список конкурентов с возможностью замены

5. **POST `/api/v1/onboarding/competitors/replace`**
   - Заменяет одного конкурента на нового
   - Принимает `competitor_id_to_replace` и `session_token`
   - Возвращает нового конкурента

6. **POST `/api/v1/onboarding/competitors/select`**
   - Сохраняет выбранных конкурентов в сессию
   - Принимает `selected_competitor_ids[]` и `session_token`
   - Возвращает подтверждение

7. **POST `/api/v1/onboarding/observation/setup`**
   - Запускает процессы наблюдения (Celery задачи)
   - Принимает `session_token`
   - Возвращает `task_id` для отслеживания прогресса
   - Задачи:
     - Поиск соцсетей конкурентов
     - Парсинг сайтов
     - Сбор новостей
     - Отслеживание маркетинга
     - SEO/SEM сигналы

8. **GET `/api/v1/onboarding/observation/status`**
   - Проверяет статус задач наблюдения
   - Принимает `task_id`
   - Возвращает `status: "pending" | "processing" | "completed"` и прогресс

9. **POST `/api/v1/onboarding/complete`**
   - Завершает онбординг
   - Принимает `session_token` и `user_id` (если регистрация была после экрана 3)
   - Создает компании и подписки на конкурентов
   - Создает Subscription с триалом на 3 дня
   - Возвращает данные для Экрана 5

**Задачи:**
- [ ] Создать файл `onboarding.py`
- [ ] Реализовать все эндпоинты
- [ ] Добавить валидацию и обработку ошибок
- [ ] Интегрировать с существующими сервисами

#### 2.2. API подписок

**Файл:** `backend/app/api/v1/endpoints/subscriptions.py`

**Эндпоинты:**

1. **GET `/api/v1/subscriptions/current`**
   - Получает текущую подписку пользователя
   - Возвращает статус, даты триала, даты подписки

2. **POST `/api/v1/subscriptions/create`**
   - Создает подписку (после оплаты)
   - Принимает `payment_provider`, `payment_subscription_id`
   - Обновляет статус Subscription на `ACTIVE`

3. **POST `/api/v1/subscriptions/cancel`**
   - Отменяет подписку
   - Устанавливает `cancelled_at` и статус `CANCELLED`

4. **GET `/api/v1/subscriptions/check-access`**
   - Проверяет доступ пользователя к функциям
   - Возвращает `has_access: bool`, `reason: str` (если нет доступа)

**Задачи:**
- [ ] Создать файл `subscriptions.py`
- [ ] Реализовать эндпоинты
- [ ] Добавить middleware для проверки подписки (опционально)

#### 2.3. Сервис подписок

**Файл:** `backend/app/services/subscription_service.py`

**Методы:**

1. `create_trial_subscription(user_id: UUID) -> Subscription`
   - Создает подписку с триалом на 3 дня
   - Устанавливает `trial_started_at`, `trial_ends_at`
   - Статус `TRIAL`

2. `check_subscription_access(user_id: UUID) -> bool`
   - Проверяет, есть ли активная подписка или триал
   - Учитывает `trial_ends_at` и `expires_at`

3. `activate_subscription(subscription_id: UUID, payment_data: dict) -> Subscription`
   - Активирует подписку после оплаты
   - Обновляет статус на `ACTIVE`
   - Устанавливает `started_at`, `expires_at` (+30 дней)

4. `cancel_subscription(subscription_id: UUID) -> Subscription`
   - Отменяет подписку
   - Устанавливает `cancelled_at`

5. `get_user_subscription(user_id: UUID) -> Optional[Subscription]`
   - Получает текущую подписку пользователя

**Задачи:**
- [ ] Создать `SubscriptionService`
- [ ] Реализовать все методы
- [ ] Добавить Celery задачу для проверки истечения триалов/подписок

#### 2.4. Celery задачи

**Файл:** `backend/app/tasks/subscriptions.py`

**Задачи:**

1. `check_expired_trials()`
   - Периодическая задача (каждый час)
   - Находит триалы, у которых `trial_ends_at < now()`
   - Обновляет статус на `EXPIRED`
   - Отправляет уведомление пользователю

2. `check_expired_subscriptions()`
   - Периодическая задача (каждый час)
   - Находит подписки, у которых `expires_at < now()`
   - Обновляет статус на `EXPIRED`

**Задачи:**
- [ ] Создать файл `subscriptions.py` в `app/tasks/`
- [ ] Реализовать задачи
- [ ] Добавить в Celery Beat расписание

---

### Фаза 3: Frontend — Компоненты онбординга

#### 3.1. Страница онбординга

**Файл:** `frontend/src/pages/OnboardingPage.tsx`

**Структура:**
- Управление состоянием текущего экрана
- Сохранение данных в localStorage (для анонимных пользователей)
- Интеграция с API онбординга
- Навигация между экранами

**Задачи:**
- [ ] Создать страницу `OnboardingPage.tsx`
- [ ] Добавить роут `/onboarding` в `App.tsx`
- [ ] Реализовать логику переключения экранов

#### 3.2. Экран 1: Ввод компании

**Файл:** `frontend/src/components/onboarding/CompanyInputStep.tsx`

**Компонент:**
- Поле ввода URL
- Placeholder: `https://example.com`
- Кнопка "Анализировать"
- Индикатор загрузки
- Обработка ошибок

**Задачи:**
- [ ] Создать компонент
- [ ] Интегрировать с API `/onboarding/company/analyze`
- [ ] Валидация URL
- [ ] Переход на Экран 2 после успешного анализа

#### 3.3. Экран 2: Карточка компании

**Файл:** `frontend/src/components/onboarding/CompanyCardStep.tsx`

**Компонент:**
- Отображение названия бренда
- Логотип (если есть)
- AI-описание (2-3 строки)
- Категория/индустрия
- Основные сигналы
- Кнопка "Далее: выбрать конкурентов"

**Задачи:**
- [ ] Создать компонент
- [ ] Получение данных из API `/onboarding/company`
- [ ] Красивый UI с карточкой компании
- [ ] Переход на Экран 3

#### 3.4. Экран 3: Выбор конкурентов

**Файл:** `frontend/src/components/onboarding/CompetitorSelectionStep.tsx`

**Компонент:**
- Заголовок "Выберите конкурентов для мониторинга"
- Сетка из 10 карточек конкурентов
- У каждой карточки:
  - Название компании
  - Логотип
  - Краткое описание
  - Сайт
  - Чекбокс "следить"
  - Крестик "заменить"
- Кнопка "Показать ещё 10" (загружает следующую порцию)
- Кнопка "Продолжить" (активна при ≥1 выборе)
- **После нажатия "Продолжить" — проверка авторизации:**
  - Если не авторизован → показывается модальное окно регистрации
  - После регистрации → продолжение онбординга

**Задачи:**
- [ ] Создать компонент
- [ ] Интегрировать с API `/onboarding/competitors/suggest`
- [ ] Реализовать замену конкурентов через API `/onboarding/competitors/replace`
- [ ] Сохранение выбранных через API `/onboarding/competitors/select`
- [ ] Интеграция с модальным окном регистрации
- [ ] Переход на Экран 4 после регистрации/авторизации

#### 3.5. Экран 4: Создание наблюдения

**Файл:** `frontend/src/components/onboarding/ObservationSetupStep.tsx`

**Компонент:**
- Заголовок "Настраиваем наблюдение за конкурентами"
- Индикатор загрузки (крутилка)
- Текст "Мы собираем данные о выбранных конкурентах. Это займёт 15–60 секунд"
- Polling статуса через API `/onboarding/observation/status`
- После завершения → кнопка "Перейти к дайджесту"

**Задачи:**
- [ ] Создать компонент
- [ ] Интегрировать с API `/onboarding/observation/setup`
- [ ] Реализовать polling статуса
- [ ] Отображение прогресса (если API возвращает)
- [ ] Переход на Экран 5 после завершения

#### 3.6. Экран 5: Подтверждение подписки

**Файл:** `frontend/src/components/onboarding/SubscriptionConfirmStep.tsx`

**Компонент:**
- Заголовок "Ваш наблюдательный центр готов"
- Список выбранных конкурентов (лого + название)
- Текст о ежедневных дайджестах
- Кнопка "Начать"
- После нажатия → завершение онбординга через API `/onboarding/complete`
- Редирект на `/dashboard` или `/dashboard-test`

**Задачи:**
- [ ] Создать компонент
- [ ] Интегрировать с API `/onboarding/complete`
- [ ] Отображение выбранных конкурентов
- [ ] Редирект после завершения

#### 3.7. Модальное окно регистрации в онбординге

**Файл:** `frontend/src/components/onboarding/OnboardingRegisterModal.tsx`

**Компонент:**
- Модальное окно с формой регистрации
- Поля: email, пароль, подтверждение пароля, имя
- Валидация
- После успешной регистрации:
  - Автоматический логин
  - Привязка `onboarding_session` к `user_id`
  - Продолжение онбординга

**Задачи:**
- [ ] Создать компонент
- [ ] Интегрировать с API регистрации
- [ ] Обработка ошибок
- [ ] Сохранение состояния онбординга при регистрации

---

### Фаза 4: Frontend — Интеграция подписок

#### 4.1. Компонент статуса подписки

**Файл:** `frontend/src/components/subscription/SubscriptionStatus.tsx`

**Компонент:**
- Отображение текущего статуса подписки
- Информация о триале (сколько дней осталось)
- Кнопка "Оформить подписку" (если триал истекает)
- Интеграция с платежной системой (будущее)

**Задачи:**
- [ ] Создать компонент
- [ ] Интегрировать с API `/subscriptions/current`
- [ ] Отображение дней до окончания триала
- [ ] Добавить в Header или Dashboard

#### 4.2. Проверка доступа к функциям

**Файл:** `frontend/src/hooks/useSubscriptionAccess.ts`

**Хук:**
- Проверяет доступ пользователя к функциям
- Использует API `/subscriptions/check-access`
- Возвращает `hasAccess: boolean`, `reason: string`
- Кэширование результата

**Задачи:**
- [ ] Создать хук
- [ ] Интегрировать в защищенные компоненты
- [ ] Показывать баннер о необходимости подписки (если нет доступа)

#### 4.3. Обновление API сервиса

**Файл:** `frontend/src/services/api.ts`

**Добавить методы:**

1. `startOnboarding() -> { session_token: string }`
2. `analyzeCompanyForOnboarding(websiteUrl: string, sessionToken: string) -> CompanyData`
3. `getOnboardingCompany(sessionToken: string) -> CompanyData`
4. `suggestCompetitorsForOnboarding(sessionToken: string) -> Competitor[]`
5. `replaceCompetitorInOnboarding(competitorId: string, sessionToken: string) -> Competitor`
6. `selectCompetitorsInOnboarding(competitorIds: string[], sessionToken: string) -> void`
7. `setupObservation(sessionToken: string) -> { task_id: string }`
8. `getObservationStatus(taskId: string) -> { status: string, progress?: number }`
9. `completeOnboarding(sessionToken: string, userId?: string) -> { subscription: Subscription }`
10. `getCurrentSubscription() -> Subscription`
11. `checkSubscriptionAccess() -> { has_access: boolean, reason?: string }`

**Задачи:**
- [ ] Добавить методы в `ApiService`
- [ ] Типизация TypeScript
- [ ] Обработка ошибок

---

### Фаза 5: Интеграция и тестирование

#### 5.1. Обновление роутинга

**Файл:** `frontend/src/App.tsx`

**Изменения:**
- [ ] Добавить роут `/onboarding` (публичный, но с проверкой сессии)
- [ ] Редирект на `/onboarding` для новых пользователей (опционально)

#### 5.2. Обновление зависимостей

**Файлы:**
- `backend/pyproject.toml` или `backend/requirements.txt`
- `frontend/package.json`

**Задачи:**
- [ ] Проверить необходимость новых зависимостей
- [ ] Добавить зависимости для платежной системы (если нужно)

#### 5.3. Миграции БД

**Задачи:**
- [ ] Создать миграцию для `subscriptions` таблицы
- [ ] Создать миграцию для `onboarding_sessions` таблицы
- [ ] Протестировать миграции на dev окружении
- [ ] Документировать процесс миграции

#### 5.4. Тестирование

**Задачи:**
- [ ] Unit тесты для `SubscriptionService`
- [ ] Unit тесты для API эндпоинтов онбординга
- [ ] E2E тесты онбординг флоу (Playwright)
- [ ] Тестирование триала (создание, истечение)
- [ ] Тестирование регистрации в середине онбординга

---

## 📝 Детальный план по файлам

### Backend файлы

#### Новые файлы:
1. `backend/app/models/subscription.py` - модель Subscription
2. `backend/app/models/onboarding.py` - модель OnboardingSession
3. `backend/app/api/v1/endpoints/onboarding.py` - API онбординга
4. `backend/app/api/v1/endpoints/subscriptions.py` - API подписок
5. `backend/app/services/subscription_service.py` - сервис подписок
6. `backend/app/tasks/subscriptions.py` - Celery задачи для подписок
7. `backend/alembic/versions/XXXX_add_subscriptions.py` - миграция для subscriptions
8. `backend/alembic/versions/XXXX_add_onboarding_sessions.py` - миграция для onboarding_sessions

#### Обновляемые файлы:
1. `backend/app/models/user.py` - добавить relationships
2. `backend/app/api/v1/api.py` - добавить роутеры для onboarding и subscriptions
3. `backend/app/core/config.py` - добавить настройки подписок (если нужно)
4. `backend/app/celery_app.py` - добавить задачи в Beat расписание

### Frontend файлы

#### Новые файлы:
1. `frontend/src/pages/OnboardingPage.tsx` - главная страница онбординга
2. `frontend/src/components/onboarding/CompanyInputStep.tsx` - экран 1
3. `frontend/src/components/onboarding/CompanyCardStep.tsx` - экран 2
4. `frontend/src/components/onboarding/CompetitorSelectionStep.tsx` - экран 3
5. `frontend/src/components/onboarding/ObservationSetupStep.tsx` - экран 4
6. `frontend/src/components/onboarding/SubscriptionConfirmStep.tsx` - экран 5
7. `frontend/src/components/onboarding/OnboardingRegisterModal.tsx` - модальное окно регистрации
8. `frontend/src/components/subscription/SubscriptionStatus.tsx` - статус подписки
9. `frontend/src/hooks/useSubscriptionAccess.ts` - хук проверки доступа

#### Обновляемые файлы:
1. `frontend/src/App.tsx` - добавить роут `/onboarding`
2. `frontend/src/services/api.ts` - добавить методы API
3. `frontend/src/types/index.ts` - добавить типы для Subscription, OnboardingSession
4. `frontend/src/components/Header.tsx` - добавить отображение статуса подписки (опционально)

---

## 🔄 Последовательность разработки (обновленная)

### ФАЗА 1: ОСНОВНОЙ ФУНКЦИОНАЛ

#### Этап 1: Backend — Интеграция GPT (1-2 дня)
1. Создать `GPTService` для генерации описаний
2. Интегрировать OpenAI API
3. Реализовать методы генерации описаний компаний и конкурентов
4. Добавить fallback на эвристические описания
5. Протестировать сервис

#### Этап 2: Backend — Модель онбординга (1 день)
1. Создать модель `OnboardingSession`
2. Создать enum `OnboardingStep`
3. Создать и применить миграцию
4. Протестировать модель

#### Этап 3: Backend — API онбординга (3-4 дня)
1. Создать эндпоинты для всех 5 экранов
2. Интегрировать с `GPTService` для генерации описаний
3. Интегрировать с существующими сервисами (CompanyInfoExtractor, CompetitorAnalysisService)
4. Реализовать сохранение состояния сессии
5. Протестировать API

#### Этап 4: Frontend — Компоненты онбординга (4-5 дней)
1. Создать страницу `OnboardingPage`
2. Реализовать Экран 1 (ввод компании)
3. Реализовать Экран 2 (карточка компании с AI-описанием)
4. Реализовать Экран 3 (выбор конкурентов с AI-описаниями) + интеграция регистрации
5. Реализовать Экран 4 (создание наблюдения)
6. Реализовать Экран 5 (подтверждение)

#### Этап 5: Frontend — Интеграция и тестирование (2-3 дня)
1. Обновить роутинг
2. E2E тесты онбординг флоу
3. Тестирование GPT интеграции
4. Исправление багов
5. Оптимизация производительности

**Итого Фаза 1: 11-15 рабочих дней**

---

### ФАЗА 2: МОНЕТИЗАЦИЯ (после завершения Фазы 1)

#### Этап 6: Backend — Модели подписок (1-2 дня)
1. Создать модель `Subscription`
2. Обновить модель `User`
3. Создать и применить миграции
4. Протестировать модели

#### Этап 7: Backend — Сервис подписок (2-3 дня)
1. Создать `SubscriptionService`
2. Реализовать методы создания триала, проверки доступа
3. Создать Celery задачи для проверки истечения
4. Протестировать сервисы

#### Этап 8: Backend — API подписок (1-2 дня)
1. Создать эндпоинты для подписок
2. Интегрировать с `SubscriptionService`
3. Обновить эндпоинт завершения онбординга для создания триала
4. Протестировать API

#### Этап 9: Frontend — Интеграция подписок (2-3 дня)
1. Создать компонент статуса подписки
2. Создать хук проверки доступа
3. Интегрировать в существующие страницы
4. Обновить API сервис

#### Этап 10: Тестирование и доработка (2-3 дня)
1. Тестирование триала
2. Тестирование истечения триала
3. Исправление багов
4. Оптимизация

**Итого Фаза 2: 8-13 рабочих дней**

**Общее время: 19-28 рабочих дней**
1. Создать модель `Subscription`
2. Создать модель `OnboardingSession`
3. Обновить модель `User`
4. Создать и применить миграции
5. Протестировать модели

### Этап 2: Backend — Сервисы (2-3 дня)
1. Создать `SubscriptionService`
2. Реализовать методы создания триала, проверки доступа
3. Создать Celery задачи для проверки истечения
4. Протестировать сервисы

### Этап 3: Backend — API онбординга (3-4 дня)
1. Создать эндпоинты для всех 5 экранов
2. Интегрировать с существующими сервисами (CompanyInfoExtractor, CompetitorAnalysisService)
3. Реализовать сохранение состояния сессии
4. Протестировать API

### Этап 4: Backend — API подписок (1-2 дня)
1. Создать эндпоинты для подписок
2. Интегрировать с `SubscriptionService`
3. Протестировать API

### Этап 5: Frontend — Компоненты онбординга (4-5 дней)
1. Создать страницу `OnboardingPage`
2. Реализовать Экран 1 (ввод компании)
3. Реализовать Экран 2 (карточка компании)
4. Реализовать Экран 3 (выбор конкурентов) + интеграция регистрации
5. Реализовать Экран 4 (создание наблюдения)
6. Реализовать Экран 5 (подтверждение подписки)

### Этап 6: Frontend — Интеграция подписок (2-3 дня)
1. Создать компонент статуса подписки
2. Создать хук проверки доступа
3. Интегрировать в существующие страницы
4. Обновить API сервис

### Этап 7: Тестирование и доработка (2-3 дня)
1. E2E тесты онбординг флоу
2. Тестирование триала
3. Исправление багов
4. Оптимизация производительности

---

## ⚠️ Важные моменты

### 1. Интеграция GPT

- **API Key:** Использовать `OPENAI_API_KEY` из `config.py`
- **Модель:** По умолчанию `gpt-4o-mini` (можно изменить через `OPENAI_MODEL`)
- **Fallback:** Если GPT недоступен или ошибка — использовать эвристические описания
- **Кэширование:** Рассмотреть кэширование результатов GPT для одинаковых компаний
- **Промпты:** Хранить промпты в конфигурации для легкой настройки

### 2. Сохранение состояния онбординга
- Использовать `session_token` для анонимных пользователей
- После регистрации привязывать сессию к `user_id`
- Сохранять данные в БД, а не только в localStorage

### 2. Регистрация в середине флоу
- После Экран 3 показывать модальное окно регистрации
- Сохранять выбранных конкурентов перед регистрацией
- После регистрации автоматически продолжать онбординг

### 3. Триал (Фаза 2)

> ⚠️ **Реализуется только в Фазе 2**
- Автоматически создавать триал на 3 дня после завершения онбординга
- Проверять истечение триала через Celery задачу
- Показывать уведомления об окончании триала

### 4. Интеграция с существующим кодом
- Использовать существующие сервисы (`CompanyInfoExtractor`, `CompetitorAnalysisService`)
- Не дублировать логику
- Сохранять совместимость с существующим функционалом

### 5. Производительность
- Оптимизировать запросы к БД
- Кэшировать данные компании и конкурентов
- Использовать асинхронные задачи для тяжелых операций

---

## 📚 Документация

После реализации необходимо обновить:
1. `README.md` - добавить описание онбординга и подписок
2. `docs/FEATURES_GUIDE.md` - добавить раздел про онбординг
3. API документация (OpenAPI) - добавить новые эндпоинты
4. `backend/env.example` - добавить пример настройки `OPENAI_API_KEY`

---

## ✅ Чек-лист готовности

### ФАЗА 1: ОСНОВНОЙ ФУНКЦИОНАЛ ✅ РЕАЛИЗОВАНО

#### Backend:
- [x] GPTService создан и интегрирован (`backend/app/services/gpt_service.py`)
- [x] OpenAI API работает корректно (с fallback на эвристические описания)
- [x] Модель OnboardingSession создана (`backend/app/models/onboarding.py`)
- [x] Миграция применена
- [x] API онбординга работает (все 9 эндпоинтов в `backend/app/api/v1/endpoints/onboarding.py`)
- [x] GPT описания генерируются для компаний и конкурентов
- [x] Мониторинг/наблюдение: базовая инфраструктура реализована (`backend/app/tasks/observation.py`)
- [x] SocialMediaExtractor реализован и работает

#### Frontend:
- [x] Все 5 экранов онбординга реализованы (`frontend/src/components/onboarding/`)
- [x] AI-описания отображаются на экранах 2 и 3
- [x] Регистрация интегрирована в онбординг (`AuthModal.tsx`, `OnboardingRegisterModal.tsx`)
- [x] Роутинг обновлен (`/onboarding`)
- [x] API сервис обновлен (`frontend/src/services/api.ts`)
- [x] ObservationSetupStep с прогресс-баром и этапами

#### Тестирование:
- [ ] Unit тесты для GPTService написаны
- [ ] Unit тесты для API онбординга написаны
- [ ] E2E тесты онбординг флоу проходят
- [x] Регистрация в онбординге работает (проверено вручную)
- [x] GPT fallback работает при ошибках (реализовано в GPTService)

---

### ФАЗА 2: МОНЕТИЗАЦИЯ

#### Backend:
- [ ] Модель Subscription создана
- [ ] Миграция применена
- [ ] SubscriptionService реализован
- [ ] API подписок работает
- [ ] Celery задачи настроены
- [ ] Триал создается после онбординга

#### Frontend:
- [ ] Компонент статуса подписки работает
- [ ] Проверка доступа к функциям работает
- [ ] Отображение дней до окончания триала

#### Тестирование:
- [ ] Триал работает корректно
- [ ] Истечение триала обрабатывается
- [ ] Проверка доступа работает

---

**Дата создания плана:** 2025-01-22  
**Дата обновления:** 2025-01-27  
**Статус:** Фаза 1 реализована, Фаза 2 в планах  
**Ориентировочное время реализации:**
- Фаза 1 (основной функционал): 11-15 рабочих дней
- Фаза 2 (монетизация): 8-13 рабочих дней
- **Общее время: 19-28 рабочих дней**

