# План рефакторинга персонализации

**Дата создания:** 2025-01-31  
**Версия:** 1.0.0  
**Статус:** План действий  
**Основан на:** `PERSONALIZATION_ARCHITECTURE_REVIEW.md`

## 📋 Обзор

Этот план описывает последовательность работ по устранению архитектурных проблем, выявленных в анализе персонализации. Основная цель — убрать дублирование кода, улучшить поддерживаемость и производительность.

**Текущая оценка:** 6.5/10  
**Целевая оценка:** 8.5/10

---

## 🎯 Цели рефакторинга

1. ✅ Устранить дублирование логики персонализации в эндпоинтах
2. ✅ Создать единую точку контроля для персонализации
3. ✅ Улучшить производительность (кеширование, оптимизация запросов)
4. ✅ Повысить тестируемость и поддерживаемость
5. ✅ Обеспечить консистентность обработки edge cases

---

## 📅 План работ по фазам

### Фаза 1: Создание PersonalizationService (КРИТИЧНО) ⚠️

**Приоритет:** Высокий  
**Оценка времени:** 1-2 дня  
**Зависимости:** Нет

#### Задачи:

1. **Создать `PersonalizationService`**
   - Файл: `backend/app/core/personalization.py`
   - Методы:
     - `get_filter_company_ids()` - единая логика получения company_ids
     - `should_return_empty()` - проверка на пустой результат
   - Интеграция с `access_control.py`

2. **Создать Dependency для FastAPI**
   - Файл: `backend/app/api/dependencies.py` (дополнить)
   - Функция: `get_personalization_service()`
   - Функция: `get_user_company_ids_for_filtering()` (опционально)

3. **Unit-тесты для PersonalizationService**
   - Файл: `backend/tests/unit/test_personalization_service.py`
   - Тесты:
     - Пользователь с компаниями
     - Пользователь без компаний
     - Анонимный пользователь
     - Явно указанные company_ids
     - Edge cases

4. **Рефакторинг эндпоинта `GET /news/`**
   - Файл: `backend/app/api/v1/endpoints/news.py`
   - Заменить дублирующуюся логику на `PersonalizationService`
   - Упростить код эндпоинта

#### Критерии готовности:
- ✅ `PersonalizationService` создан и протестирован
- ✅ `GET /news/` использует сервис
- ✅ Все существующие тесты проходят
- ✅ Код стал короче и понятнее

---

### Фаза 2: Рефакторинг остальных эндпоинтов новостей

**Приоритет:** Высокий  
**Оценка времени:** 1 день  
**Зависимости:** Фаза 1

#### Задачи:

1. **Рефакторинг `GET /news/search`**
   - Файл: `backend/app/api/v1/endpoints/news.py`
   - Использовать `PersonalizationService`
   - Убрать дублирование логики

2. **Рефакторинг `GET /news/category/{category_name}`**
   - Файл: `backend/app/api/v1/endpoints/news.py`
   - Использовать `PersonalizationService`
   - Убрать дублирование логики

3. **Рефакторинг `GET /news/stats`** (если нужно)
   - Проверить консистентность с другими эндпоинтами
   - Привести к единому стилю

4. **Обновить `DigestService`** (опционально)
   - Файл: `backend/app/domains/notifications/services/digest_service.py`
   - Использовать `PersonalizationService` для консистентности

#### Критерии готовности:
- ✅ Все эндпоинты новостей используют `PersonalizationService`
- ✅ Дублирование кода устранено
- ✅ Все существующие тесты проходят
- ✅ Код консистентен во всех эндпоинтах

---

### Фаза 3: Оптимизация производительности

**Приоритет:** Средний  
**Оценка времени:** 1-2 дня  
**Зависимости:** Фаза 1, Фаза 2

#### Задачи:

1. **Кеширование `get_user_company_ids`**
   - Файл: `backend/app/core/access_control.py` или `personalization.py`
   - Варианты:
     - In-memory cache (TTLCache) - проще, быстрее
     - Redis cache - масштабируемее
   - TTL: 5 минут (настраиваемо)
   - Инвалидация при изменении компаний пользователя

2. **Оптимизация `check_news_access`**
   - Файл: `backend/app/core/access_control.py`
   - Убрать N+1 запросы
   - Использовать `join` или `selectinload`
   - Один запрос вместо двух

3. **Ранняя проверка пустых списков**
   - Убедиться, что проверка `parsed_company_ids == []` происходит ДО запроса к БД
   - Оптимизировать в `search_news` (если еще не сделано)

#### Критерии готовности:
- ✅ Кеширование работает и тестируется
- ✅ `check_news_access` оптимизирован (один запрос)
- ✅ Ранние проверки работают везде
- ✅ Производительность улучшена (метрики)

---

### Фаза 4: Улучшение тестирования

**Приоритет:** Средний  
**Оценка времени:** 1-2 дня  
**Зависимости:** Фаза 1, Фаза 2

#### Задачи:

1. **E2E тесты для эндпоинтов новостей**
   - Файл: `backend/tests/integration/test_news_endpoints_personalization.py`
   - Тесты:
     - Пользователь с компаниями видит свои новости
     - Пользователь без компаний видит пустой список
     - Анонимный пользователь видит глобальные новости
     - Явно указанные company_ids работают корректно
     - Изоляция данных между пользователями

2. **Тесты для кеширования** (если добавлено)
   - Проверка работы кеша
   - Проверка инвалидации

3. **Тесты производительности** (опционально)
   - Benchmark для `get_user_company_ids` с кешем и без
   - Benchmark для `check_news_access` до и после оптимизации

#### Критерии готовности:
- ✅ E2E тесты покрывают основные сценарии
- ✅ Все тесты проходят
- ✅ Покрытие тестами увеличено

---

### Фаза 5: Документация и финализация

**Приоритет:** Низкий  
**Оценка времени:** 0.5 дня  
**Зависимости:** Все предыдущие фазы

#### Задачи:

1. **Обновить документацию**
   - Обновить `README.md` с описанием `PersonalizationService`
   - Добавить примеры использования
   - Описать архитектурные решения

2. **Обновить архитектурную документацию**
   - Обновить `PERSONALIZATION_ARCHITECTURE_REVIEW.md` с результатами
   - Создать `PERSONALIZATION_REFACTORING_RESULTS.md` с итогами

3. **Code review и финальная проверка**
   - Проверить все изменения
   - Убедиться в отсутствии регрессий
   - Проверить соответствие стандартам кода

#### Критерии готовности:
- ✅ Документация обновлена
- ✅ README содержит информацию о новом функционале
- ✅ Все изменения задокументированы

---

## 📊 Метрики успеха

### До рефакторинга:
- Дублирование логики: 3+ места
- Консистентность: 5/10
- Производительность: 6/10
- Поддерживаемость: 5/10
- Общая оценка: 6.5/10

### После рефакторинга (целевые):
- Дублирование логики: 0 (единая точка)
- Консистентность: 9/10
- Производительность: 8/10 (с кешированием)
- Поддерживаемость: 8/10
- Общая оценка: 8.5/10

---

## 🔧 Технические детали

### Структура файлов

```
backend/
├── app/
│   ├── core/
│   │   ├── access_control.py      # Существующий (оптимизировать)
│   │   └── personalization.py     # НОВЫЙ - PersonalizationService
│   ├── api/
│   │   ├── dependencies.py        # Дополнить - dependencies
│   │   └── v1/
│   │       └── endpoints/
│   │           └── news.py        # Рефакторинг
│   └── domains/
│       └── notifications/
│           └── services/
│               └── digest_service.py  # Опциональный рефакторинг
└── tests/
    ├── unit/
    │   ├── test_access_control.py           # Существующий
    │   └── test_personalization_service.py # НОВЫЙ
    └── integration/
        ├── test_personalization_isolation.py  # Существующий
        ├── test_personalization_security.py   # Существующий
        └── test_news_endpoints_personalization.py  # НОВЫЙ
```

### Зависимости (если нужны)

Для кеширования:
- `cachetools` (для in-memory cache) - легковесно
- Или `redis` (если уже используется в проекте)

---

## ⚠️ Риски и митигация

### Риск 1: Регрессии в существующей функциональности
**Митигация:**
- Тщательное тестирование перед каждым изменением
- Постепенный рефакторинг (по одному эндпоинту)
- Сохранение всех существующих тестов

### Риск 2: Проблемы с производительностью кеша
**Митигация:**
- Начать с простого in-memory кеша
- Добавить метрики для мониторинга
- Настроить разумный TTL

### Риск 3: Сложность миграции существующего кода
**Митигация:**
- Создать `PersonalizationService` параллельно
- Постепенно мигрировать эндпоинты
- Сохранить обратную совместимость

---

## 🚀 Быстрый старт

### Начать с Фазы 1:

1. Создать `backend/app/core/personalization.py`
2. Написать unit-тесты
3. Рефакторить один эндпоинт (`GET /news/`)
4. Проверить, что все работает
5. Продолжить с остальными эндпоинтами

### Команды для проверки:

```bash
# Запустить тесты
cd backend
poetry run pytest tests/unit/test_personalization_service.py -v
poetry run pytest tests/integration/test_news_endpoints_personalization.py -v

# Проверить линтер
poetry run ruff check app/core/personalization.py
poetry run mypy app/core/personalization.py
```

---

## 📝 Чеклист выполнения

### Фаза 1
- [ ] Создан `PersonalizationService`
- [ ] Написаны unit-тесты
- [ ] Рефакторинг `GET /news/`
- [ ] Все тесты проходят

### Фаза 2
- [ ] Рефакторинг `GET /news/search`
- [ ] Рефакторинг `GET /news/category/{category_name}`
- [ ] Проверка `GET /news/stats`
- [ ] Все тесты проходят

### Фаза 3
- [ ] Добавлено кеширование
- [ ] Оптимизирован `check_news_access`
- [ ] Ранние проверки везде
- [ ] Метрики производительности

### Фаза 4
- [ ] E2E тесты написаны
- [ ] Тесты кеширования (если применимо)
- [ ] Покрытие увеличено

### Фаза 5
- [ ] README обновлен
- [ ] Документация обновлена
- [ ] Code review пройден

---

## 📚 Связанные документы

- `docs/ANALYSIS/PERSONALIZATION_ARCHITECTURE_REVIEW.md` - исходный анализ
- `docs/ANALYSIS/PERSONALIZATION_IMPLEMENTATION_PLAN.md` - план реализации
- `docs/ANALYSIS/PERSONALIZATION_RISKS.md` - анализ рисков
- `backend/app/core/access_control.py` - текущая реализация

---

**Автор:** AI Code Review  
**Дата:** 2025-01-31  
**Статус:** Готов к выполнению




