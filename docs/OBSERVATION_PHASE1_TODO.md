# TODO List: Этап 1 - Критические блокеры

**Дата создания:** 2025-01-27  
**Оценка времени:** 1-2 дня  
**Приоритет:** 🔴 КРИТИЧЕСКИЙ (блокирует дальнейшую работу)

---

## 📋 Задачи

### 1. Обновление модели Company

#### 1.1. Добавить поля соцсетей в модель
- [ ] **Задача 1.1**: Добавить поля соцсетей в модель `Company`
  - **Файл:** `backend/app/models/company.py`
  - **Действия:**
    1. Добавить поле `facebook_url: Mapped[Optional[str]] = mapped_column(String(500))`
    2. Добавить поле `instagram_url: Mapped[Optional[str]] = mapped_column(String(500))`
    3. Добавить поле `linkedin_url: Mapped[Optional[str]] = mapped_column(String(500))`
    4. Добавить поле `youtube_url: Mapped[Optional[str]] = mapped_column(String(500))`
    5. Добавить поле `tiktok_url: Mapped[Optional[str]] = mapped_column(String(500))`
  - **Проверка:** Убедиться, что миграция `a1b2c3d4e5f7_add_social_media_fields_to_companies.py` применена
  - **Примечание:** Поля уже добавлены в миграцию, нужно только добавить их в модель Python

#### 1.2. Добавить методы для извлечения handles (опционально)
- [ ] **Задача 1.2**: Добавить вспомогательные методы в модель `Company`
  - **Методы:**
    - `get_facebook_handle() -> Optional[str]` - извлечь handle из facebook_url
    - `get_instagram_handle() -> Optional[str]` - извлечь handle из instagram_url
    - `get_linkedin_handle() -> Optional[str]` - извлечь handle из linkedin_url
    - `get_youtube_handle() -> Optional[str]` - извлечь handle из youtube_url
    - `get_tiktok_handle() -> Optional[str]` - извлечь handle из tiktok_url
  - **Приоритет:** Низкий (можно сделать позже, если понадобится)

#### 1.3. Тестирование модели Company
- [ ] **Задача 1.3**: Протестировать обновленную модель
  - Создать тестовую компанию через API с полями соцсетей
  - Проверить сохранение и чтение данных
  - Проверить, что поля корректно сериализуются в JSON

---

### 2. Расширение SourceType enum

#### 2.1. Добавить новые типы в enum
- [ ] **Задача 2.1**: Добавить новые типы в `SourceType` enum
  - **Файл:** `backend/app/models/news.py`
  - **Действия:**
    1. Добавить `FACEBOOK = "facebook"` в enum
    2. Добавить `INSTAGRAM = "instagram"` в enum
    3. Добавить `LINKEDIN = "linkedin"` в enum
    4. Добавить `YOUTUBE = "youtube"` в enum
    5. Добавить `TIKTOK = "tiktok"` в enum
  - **Проверка:** Убедиться, что миграция `b2c3d4e5f6g7_add_social_media_source_types.py` применена
  - **Примечание:** Миграция уже создана, нужно только добавить значения в Python enum

#### 2.2. Обновить метод get_descriptions()
- [ ] **Задача 2.2**: Обновить метод `get_descriptions()` в `SourceType`
  - **Добавить описания:**
    - `cls.FACEBOOK: "Facebook posts and pages"`
    - `cls.INSTAGRAM: "Instagram posts and profiles"`
    - `cls.LINKEDIN: "LinkedIn company pages and posts"`
    - `cls.YOUTUBE: "YouTube channels and videos"`
    - `cls.TIKTOK: "TikTok profiles and videos"`

#### 2.3. Тестирование SourceType
- [ ] **Задача 2.3**: Протестировать новые типы SourceType
  - Создать тестовую новость с типом `FACEBOOK`
  - Проверить, что enum корректно сериализуется в JSON
  - Проверить валидацию через Pydantic схемы

---

### 3. Проверка миграций

#### 3.1. Проверить применение миграций
- [ ] **Задача 3.1**: Убедиться, что миграции применены
  - Проверить миграцию `a1b2c3d4e5f7_add_social_media_fields_to_companies.py`
  - Проверить миграцию `b2c3d4e5f6g7_add_social_media_source_types.py`
  - Выполнить: `cd backend && poetry run alembic upgrade head`
  - Проверить структуру таблиц в БД

#### 3.2. Проверить соответствие модели и БД
- [ ] **Задача 3.2**: Проверить соответствие модели Company и таблицы в БД
  - Убедиться, что все поля из миграции присутствуют в модели
  - Проверить типы данных (String(500) для URL полей)

---

## ✅ Критерии завершения этапа

- [x] Все поля соцсетей добавлены в модель `Company` ✅
- [x] Новые типы добавлены в `SourceType` enum ✅
- [x] Метод `get_descriptions()` обновлен ✅
- [x] Миграции применены и проверены ✅
- [x] Модель Company протестирована (создание/чтение с полями соцсетей) ✅ (работает через API)
- [x] SourceType протестирован (создание новостей с новыми типами) ✅ (работает через API)
- [x] Нет ошибок при запуске приложения ✅
- [x] API endpoints корректно работают с новыми полями ✅

**Этап 1 завершен полностью!** ✅

---

## 🔍 Проверочный чек-лист

Перед переходом к следующему этапу убедитесь:

1. **Модель Company:**
   - [ ] Поля `facebook_url`, `instagram_url`, `linkedin_url`, `youtube_url`, `tiktok_url` присутствуют в модели
   - [ ] Поля имеют правильный тип `Mapped[Optional[str]]`
   - [ ] Поля корректно сохраняются в БД
   - [ ] Поля корректно читаются из БД
   - [ ] Поля корректно сериализуются в JSON через Pydantic схемы

2. **SourceType enum:**
   - [ ] Значения `FACEBOOK`, `INSTAGRAM`, `LINKEDIN`, `YOUTUBE`, `TIKTOK` присутствуют в enum
   - [ ] Метод `get_descriptions()` возвращает описания для всех новых типов
   - [ ] Enum корректно работает с PostgreSQL (нет ошибок при сохранении)
   - [ ] Enum корректно сериализуется в JSON

3. **Миграции:**
   - [ ] Миграция для полей соцсетей применена
   - [ ] Миграция для типов источников применена
   - [ ] Структура БД соответствует моделям

4. **Тестирование:**
   - [ ] Можно создать компанию с полями соцсетей через API
   - [ ] Можно создать новость с новым типом источника через API
   - [ ] Нет ошибок в логах при работе с новыми полями

---

## 📝 Примечания

- **Важно:** Поля соцсетей уже добавлены в миграцию и схемы Pydantic, нужно только добавить их в модель Python
- **Важно:** Типы источников уже добавлены в миграцию и frontend типы, нужно только добавить их в Python enum
- **Рекомендация:** После завершения этапа проверить работу через API endpoints `/api/v1/companies/` и `/api/v1/news/`

---

## 🚀 Следующий этап

После завершения Этапа 1 переходим к **Этапу 2: Основная функциональность** - создание сервисов мониторинга.
