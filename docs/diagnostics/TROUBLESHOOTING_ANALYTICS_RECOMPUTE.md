# Диагностика проблем с пересчётом аналитики

## 🔴 Возможные проблемы при нажатии "Recompute"

### 1. **Celery Worker не запущен**

**Симптомы:**
- Кнопка "Recompute" работает, показывает "Analytics recompute queued"
- Но данные не появляются даже через 5+ секунд
- В логах backend нет записей о выполнении задачи

**Проверка:**
```bash
# Проверить, запущен ли Celery worker
celery -A app.celery_app inspect active

# Проверить очередь analytics
celery -A app.celery_app inspect reserved -Q analytics
```

**Решение:**
```bash
# Запустить Celery worker с очередью analytics
celery -A app.celery_app worker --loglevel=info -Q analytics,celery
```

---

### 2. **Redis недоступен**

**Симптомы:**
- При нажатии "Recompute" появляется ошибка:
  ```
  "Analytics queue is unavailable. Please ensure Celery worker and Redis are running."
  ```
- HTTP 503 Service Unavailable

**Проверка:**
```bash
# Проверить подключение к Redis
redis-cli ping
# Должен вернуть: PONG
```

**Решение:**
```bash
# Запустить Redis (если через Docker)
docker-compose up -d redis

# Или проверить переменные окружения
echo $CELERY_BROKER_URL
echo $CELERY_RESULT_BACKEND
# Должны указывать на Redis: redis://localhost:6379/0
```

---

### 3. **Нет данных для компании**

**Симптомы:**
- Пересчёт выполняется успешно
- Но snapshot создаётся с нулевыми значениями:
  - `news_total: 0`
  - `impact_score: 0.0`
  - `pricing_changes: 0`
  - `feature_updates: 0`

**Проверка:**
```sql
-- Проверить новости для компании
SELECT COUNT(*) FROM news_items 
WHERE company_id = 'YOUR_COMPANY_ID';

-- Проверить change events
SELECT COUNT(*) FROM competitor_change_events 
WHERE company_id = 'YOUR_COMPANY_ID' 
AND processing_status = 'success';
```

**Решение:**
1. Убедиться, что есть новости с заполненными полями:
   - `sentiment` (POSITIVE/NEGATIVE/NEUTRAL)
   - `priority_score`
   - `published_at`
2. Убедиться, что есть change events со статусом `SUCCESS`
3. Если данных нет — запустить скрейпер для компании

---

### 4. **Задача в очереди, но не выполняется**

**Симптомы:**
- Задача поставлена в очередь (получен `task_id`)
- Но worker не обрабатывает её
- В логах worker нет записей о задаче

**Проверка:**
```bash
# Проверить зарезервированные задачи
celery -A app.celery_app inspect reserved

# Проверить активные задачи
celery -A app.celery_app inspect active

# Проверить статистику worker
celery -A app.celery_app inspect stats
```

**Решение:**
1. Убедиться, что worker слушает очередь `analytics`:
   ```bash
   celery -A app.celery_app worker -Q analytics,celery
   ```
2. Проверить конфигурацию в `celery_app.py`:
   ```python
   task_routes = {
       'app.tasks.analytics.*': {'queue': 'analytics'},
   }
   ```

---

### 5. **Ошибки при выполнении задачи**

**Симптомы:**
- Задача выполняется, но падает с ошибкой
- В логах backend видны исключения
- Snapshot не создаётся

**Типичные ошибки:**

#### 5.1. Ошибка подключения к БД
```
asyncpg.exceptions.InterfaceError: connection was closed
```
**Решение:** Проверить настройки БД, перезапустить worker

#### 5.2. Ошибка дедупликации
```
Redis connection error
```
**Решение:** Проверить доступность Redis

#### 5.3. Ошибка при создании snapshot
```
IntegrityError: duplicate key value violates unique constraint
```
**Решение:** Проверить уникальные ограничения в таблице `company_analytics_snapshots`

**Проверка логов:**
```bash
# Логи Celery worker
tail -f logs/celery.log

# Или если запущен через Docker
docker-compose logs -f celery-worker
```

---

### 6. **Автоматическое обновление не срабатывает**

**Симптомы:**
- Пересчёт выполнен успешно
- Но UI не обновляется автоматически через 5 секунд
- Нужно обновлять страницу вручную

**Причина:**
- `pendingTaskId` не устанавливается
- `useEffect` с таймером не срабатывает
- `refetchAnalyticsInsights()` не вызывается

**Проверка:**
1. Открыть DevTools → Console
2. Проверить, что `task_id` получен при нажатии "Recompute"
3. Проверить, что через 5 секунд отправляется запрос на `/analytics/companies/{id}/impact/latest`

**Решение:**
- Проверить логику в `CompetitorAnalysisPage.tsx` (строки 257-272)
- Убедиться, что `setPendingTaskId(task_id)` вызывается

---

### 7. **Snapshot создаётся, но не отображается**

**Симптомы:**
- Пересчёт выполнен успешно
- В БД есть snapshot
- Но UI показывает "Аналитика ещё не построена"

**Проверка:**
```sql
-- Проверить наличие snapshot
SELECT id, company_id, period, period_start, impact_score 
FROM company_analytics_snapshots 
WHERE company_id = 'YOUR_COMPANY_ID' 
AND period = 'daily'
ORDER BY period_start DESC 
LIMIT 1;
```

**Возможные причины:**
1. Неправильный `period` (запрашивается `daily`, а создан `weekly`)
2. Snapshot для другого периода
3. Проблема с кэшем React Query

**Решение:**
1. Проверить параметр `period` в запросе (должен быть `daily`)
2. Очистить кэш React Query:
   ```javascript
   queryClient.invalidateQueries({ queryKey: ['competitor-analysis', 'company-analytics'] })
   ```
3. Обновить страницу

---

## ✅ Чек-лист диагностики

При появлении ошибки "Аналитика ещё не построена":

- [ ] Проверить, запущен ли Celery worker: `celery -A app.celery_app inspect active`
- [ ] Проверить доступность Redis: `redis-cli ping`
- [ ] Проверить наличие новостей для компании в БД
- [ ] Проверить наличие change events со статусом `SUCCESS`
- [ ] Проверить логи backend при нажатии "Recompute"
- [ ] Проверить логи Celery worker на наличие ошибок
- [ ] Проверить, что worker слушает очередь `analytics`
- [ ] Проверить наличие snapshot в БД после пересчёта
- [ ] Проверить параметр `period` в запросе (должен совпадать с созданным snapshot)
- [ ] Проверить консоль браузера на наличие ошибок API

---

## 🔧 Быстрое решение

Если ничего не помогает:

1. **Перезапустить инфраструктуру:**
   ```bash
   docker-compose restart redis celery-worker
   ```

2. **Очистить кэш Redis:**
   ```bash
   redis-cli FLUSHDB
   ```

3. **Запустить пересчёт вручную через API:**
   ```bash
   curl -X POST "http://localhost:8000/api/v2/analytics/companies/{id}/recompute?period=daily&lookback=60" \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```

4. **Проверить результат:**
   ```bash
   curl "http://localhost:8000/api/v2/analytics/companies/{id}/impact/latest?period=daily" \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```

---

## 📝 Файлы для проверки

- `backend/app/celery_app.py` — конфигурация Celery и очередей
- `backend/app/tasks/analytics.py` — задачи пересчёта
- `backend/app/domains/analytics/services/snapshot_service.py` — логика создания snapshots
- `backend/app/api/v2/endpoints/analytics.py` — API endpoints
- `frontend/src/pages/CompetitorAnalysisPage.tsx` — логика UI и обновления данных




