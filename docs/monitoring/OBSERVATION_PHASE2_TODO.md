# TODO List: Этап 2 - Основная функциональность (Сервисы мониторинга)

**Дата создания:** 2025-01-27  
**Оценка времени:** 6-8 дней  
**Приоритет:** 🟡 ВАЖНЫЙ (основа функциональности)

---

## 📋 Задачи

### 1. WebsiteStructureMonitor (2-3 дня)

#### 1.1. Создание сервиса
- [ ] **Задача 1.1**: Создать сервис `WebsiteStructureMonitor`
  - **Файл:** `backend/app/services/website_structure_monitor.py`
  - **Класс:** `WebsiteStructureMonitor`
  - **Методы:**
    - `async def capture_website_snapshot(website_url: str) -> Dict[str, Any]` - захват снимка структуры
    - `async def detect_structure_changes(previous_snapshot: Dict, current_snapshot: Dict) -> Dict[str, Any]` - сравнение снимков
    - `async def extract_key_pages(website_url: str) -> List[str]` - извлечение ключевых страниц
  - **Зависимости:** `httpx`, `beautifulsoup4`, `lxml`

#### 1.2. Извлечение структуры сайта
- [ ] **Задача 1.2**: Реализовать извлечение навигационного меню
  - Парсинг `<nav>`, `<header>`, `<menu>` элементов
  - Извлечение всех ссылок из навигации
  - Сохранение структуры меню (иерархия, уровни)

- [ ] **Задача 1.3**: Реализовать поиск основных страниц
  - Поиск страниц: `/pricing`, `/features`, `/about`, `/blog`, `/news`, `/careers`
  - Автоматическое определение URL основных страниц
  - Сохранение списка найденных страниц

- [ ] **Задача 1.4**: Реализовать извлечение метаданных страниц
  - `<title>`, `<meta name="description">`, `<meta name="keywords">`
  - Open Graph теги (`og:title`, `og:description`, `og:image`)
  - Twitter Card теги
  - Сохранение метаданных для каждой страницы

- [ ] **Задача 1.5**: Реализовать извлечение структуры HTML
  - Основные секции: `<header>`, `<main>`, `<footer>`, `<aside>`
  - Структура контента (заголовки, списки, таблицы)
  - Сохранение структуры для сравнения

#### 1.3. Сравнение снимков
- [ ] **Задача 1.6**: Реализовать diff по структуре навигации
  - Сравнение списка ссылок в навигации
  - Обнаружение новых/удаленных/измененных ссылок
  - Возврат детального diff

- [ ] **Задача 1.7**: Реализовать diff по основным страницам
  - Сравнение списка найденных страниц
  - Обнаружение новых/удаленных страниц
  - Сравнение метаданных страниц

- [ ] **Задача 1.8**: Реализовать diff по метаданным
  - Сравнение title, description, keywords
  - Обнаружение изменений в Open Graph тегах
  - Возврат списка изменений

---

### 2. MarketingChangeDetector (2-3 дня)

#### 2.1. Создание сервиса
- [ ] **Задача 2.1**: Создать сервис `MarketingChangeDetector`
  - **Файл:** `backend/app/services/marketing_change_detector.py`
  - **Класс:** `MarketingChangeDetector`
  - **Методы:**
    - `async def detect_pricing_changes(company_id: UUID) -> List[Dict]` - использовать существующий `CompetitorChangeService`
    - `async def detect_banner_changes(website_url: str) -> List[Dict]` - детекция изменений баннеров
    - `async def detect_landing_page_changes(landing_url: str) -> List[Dict]` - детекция изменений лендингов
    - `async def detect_new_products(product_page_url: str) -> List[Dict]` - детекция новых продуктов
    - `async def detect_job_postings(careers_url: str) -> List[Dict]` - детекция новых вакансий

#### 2.2. Детекция изменений баннеров
- [ ] **Задача 2.2**: Реализовать детекцию изменений баннеров
  - Загрузка главной страницы
  - Поиск изображений баннеров (hero section, carousel, banner)
  - Вычисление hash изображений (MD5/SHA256)
  - Сравнение с предыдущими hash
  - Возврат списка изменений

#### 2.3. Детекция изменений лендингов
- [ ] **Задача 2.3**: Реализовать детекцию изменений лендингов
  - Загрузка лендинг-страницы
  - Извлечение структуры и контента
  - Сравнение с предыдущим снимком
  - Обнаружение изменений в тексте, структуре, изображениях
  - Возврат детального diff

#### 2.4. Детекция новых продуктов
- [ ] **Задача 2.4**: Реализовать детекцию новых продуктов
  - Парсинг страницы продуктов (`/products`, `/features`, `/solutions`)
  - Извлечение списка продуктов (названия, описания, цены)
  - Сравнение с предыдущим списком
  - Обнаружение новых/удаленных продуктов
  - Возврат списка изменений

#### 2.5. Детекция новых вакансий
- [ ] **Задача 2.5**: Реализовать детекцию новых вакансий
  - Парсинг страницы careers (`/careers`, `/jobs`, `/hiring`)
  - Извлечение списка вакансий (названия, локации, департаменты)
  - Сравнение с предыдущим списком
  - Обнаружение новых/закрытых вакансий
  - Возврат списка изменений

#### 2.6. Интеграция с CompetitorChangeEvent
- [ ] **Задача 2.6**: Интегрировать с системой событий
  - Создавать `CompetitorChangeEvent` для каждого обнаруженного изменения
  - Использовать `SourceType` для категоризации (например, `MARKETING_BANNER`, `MARKETING_LANDING`)
  - Сохранять детали изменений в `change_data`
  - Устанавливать правильный `change_type`

---

### 3. SEOSignalCollector (1-2 дня)

#### 3.1. Создание сервиса
- [ ] **Задача 3.1**: Создать сервис `SEOSignalCollector`
  - **Файл:** `backend/app/services/seo_signal_collector.py`
  - **Класс:** `SEOSignalCollector`
  - **Методы:**
    - `async def collect_seo_signals(website_url: str) -> Dict[str, Any]` - основной метод сбора
    - `def extract_meta_tags(soup: BeautifulSoup) -> Dict[str, str]` - извлечение meta тегов
    - `def extract_structured_data(soup: BeautifulSoup) -> Dict[str, Any]` - извлечение structured data
    - `async def check_robots_txt(website_url: str) -> Dict[str, Any]` - анализ robots.txt
    - `async def check_sitemap(website_url: str) -> List[str]` - анализ sitemap.xml

#### 3.2. Извлечение meta тегов
- [ ] **Задача 3.2**: Реализовать извлечение meta тегов
  - `<title>`, `<meta name="description">`, `<meta name="keywords">`
  - Open Graph теги (`og:title`, `og:description`, `og:image`, `og:url`, `og:type`)
  - Twitter Card теги
  - Canonical URL (`<link rel="canonical">`)
  - Hreflang теги (`<link rel="alternate" hreflang="...">`)

#### 3.3. Извлечение structured data
- [ ] **Задача 3.3**: Реализовать извлечение structured data
  - JSON-LD скрипты (`<script type="application/ld+json">`)
  - Schema.org разметка (Organization, Product, Article и т.д.)
  - Микроданные (microdata)
  - RDFa разметка

#### 3.4. Анализ robots.txt и sitemap
- [ ] **Задача 3.4**: Реализовать анализ robots.txt
  - Загрузка `/robots.txt`
  - Парсинг правил для User-agent
  - Извлечение ссылок на sitemap
  - Возврат структурированных данных

- [ ] **Задача 3.5**: Реализовать анализ sitemap.xml
  - Загрузка sitemap.xml (из robots.txt или стандартный путь)
  - Парсинг XML sitemap
  - Извлечение всех URL из sitemap
  - Поддержка sitemap index (вложенные sitemaps)
  - Возврат списка URL

#### 3.5. Сравнение SEO сигналов
- [ ] **Задача 3.6**: Реализовать сравнение SEO сигналов
  - Diff по meta тегам (изменения в title, description, keywords)
  - Diff по structured data (новые/измененные схемы)
  - Изменения в sitemap (новые/удаленные URL)
  - Возврат детального diff

---

### 4. PressReleaseScraper (1 день)

#### 4.1. Создание сервиса
- [ ] **Задача 4.1**: Создать специализированный `PressReleaseScraper`
  - **Файл:** `backend/app/scrapers/press_release_scraper.py`
  - **Класс:** `PressReleaseScraper`
  - **Методы:**
    - `async def find_press_release_page(website_url: str) -> Optional[str]` - поиск страницы пресс-релизов
    - `async def scrape_press_releases(press_url: str) -> List[Dict]` - парсинг пресс-релизов

#### 4.2. Поиск страницы пресс-релизов
- [ ] **Задача 4.2**: Реализовать поиск страницы пресс-релизов
  - Поиск по стандартным путям: `/press`, `/press-releases`, `/newsroom`, `/media`
  - Проверка доступности страниц
  - Возврат URL найденной страницы или None

#### 4.3. Парсинг пресс-релизов
- [ ] **Задача 4.3**: Реализовать парсинг пресс-релизов
  - Парсинг списка пресс-релизов (заголовки, даты, ссылки)
  - Загрузка детальной страницы каждого релиза
  - Извлечение: title, content, published_at, author
  - Возврат списка словарей с данными

#### 4.4. Интеграция с NewsIngestionService
- [ ] **Задача 4.4**: Интегрировать с системой новостей
  - Использовать `NewsIngestionService` для сохранения
  - Устанавливать `source_type=SourceType.PRESS_RELEASE`
  - Категоризация как `PRESS_RELEASE`
  - Сохранение в таблицу `news_items`

---

### 5. Обновление Celery задач (1-2 дня)

#### 5.1. Обновление capture_website_structure_async
- [ ] **Задача 5.1**: Обновить `capture_website_structure_async()` в `observation.py`
  - Импортировать `WebsiteStructureMonitor`
  - Вызвать `capture_website_snapshot()` для компании
  - Сохранить снимок в `CompetitorMonitoringMatrix.website_sources`
  - Обновить `last_updated` в матрице

#### 5.2. Обновление scrape_press_releases_async
- [ ] **Задача 5.2**: Обновить `scrape_press_releases_async()` в `observation.py`
  - Импортировать `PressReleaseScraper`
  - Найти страницу пресс-релизов через `find_press_release_page()`
  - Спарсить пресс-релизы через `scrape_press_releases()`
  - Сохранить через `NewsIngestionService` с типом `PRESS_RELEASE`
  - Обновить `CompetitorMonitoringMatrix.news_sources`

#### 5.3. Обновление detect_marketing_changes_async
- [ ] **Задача 5.3**: Обновить `detect_marketing_changes_async()` в `observation.py`
  - Импортировать `MarketingChangeDetector`
  - Вызвать методы детекции для всех типов изменений
  - Для каждого обнаруженного изменения создать `CompetitorChangeEvent`
  - Сохранить результаты в `CompetitorMonitoringMatrix.marketing_sources`

#### 5.4. Обновление collect_seo_signals_async
- [ ] **Задача 5.4**: Обновить `collect_seo_signals_async()` в `observation.py`
  - Импортировать `SEOSignalCollector`
  - Вызвать `collect_seo_signals()` для компании
  - Сохранить сигналы в `CompetitorMonitoringMatrix.seo_signals`
  - Обновить `last_updated` в матрице

#### 5.5. Создание задачи compare_website_structures
- [ ] **Задача 5.5**: Создать задачу `compare_website_structures` в `observation.py`
  - Метод: `@celery_app.task compare_website_structures.delay(company_id: str)`
  - Загрузить текущий и предыдущий снимки из `CompetitorMonitoringMatrix`
  - Вызвать `detect_structure_changes()` из `WebsiteStructureMonitor`
  - Создать `CompetitorChangeEvent` для каждого изменения
  - Обновить снимок в матрице

#### 5.6. Создание задачи compare_seo_signals
- [ ] **Задача 5.6**: Создать задачу `compare_seo_signals` в `observation.py`
  - Метод: `@celery_app.task compare_seo_signals.delay(company_id: str)`
  - Загрузить текущие и предыдущие SEO сигналы из матрицы
  - Вызвать метод сравнения из `SEOSignalCollector`
  - Создать `CompetitorChangeEvent` для каждого изменения
  - Обновить сигналы в матрице

---

## ✅ Критерии завершения этапа

- [ ] Все 4 сервиса созданы и реализованы
- [ ] Все методы сервисов работают корректно
- [ ] Celery задачи обновлены и используют реальные сервисы
- [ ] Созданы периодические задачи сравнения
- [ ] Интеграция с `CompetitorChangeEvent` работает
- [ ] Данные сохраняются в `CompetitorMonitoringMatrix`
- [ ] Нет ошибок линтера
- [ ] Код следует существующим паттернам проекта

---

## 📝 Примечания

- **Зависимости:** Все сервисы должны использовать существующие библиотеки проекта (`httpx`, `beautifulsoup4`)
- **Обработка ошибок:** Все сервисы должны корректно обрабатывать ошибки (таймауты, недоступные страницы, парсинг ошибки)
- **Логирование:** Использовать `loguru` для логирования всех операций
- **Персонализация:** Учитывать `user_id` при работе с компаниями (использовать существующие проверки доступа)

---

## 🚀 Следующий этап

После завершения Этапа 2 переходим к **Этапу 3: Интеграция** - интеграция с системой уведомлений и периодические задачи.
