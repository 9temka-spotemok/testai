# План реализации процессов наблюдения за конкурентами

## 📋 Обзор

Реализация реальных процессов наблюдения за конкурентами для онбординга. Сейчас процессы только симулируются (создается task_id, но реальные задачи не запускаются).

## 🎯 Цели

1. **Поиск соцсетей конкурентов** (FB/IG/TT/X/LinkedIn/YouTube)
2. **Парсинг сайтов** (изменения в структуре/контенте)
3. **Сбор новостей + фирменных пресс-релизов**
4. **Отслеживание маркетинговых изменений** (акции, баннеры, лендинги, новые продукты, вакансии)
5. **Сбор сигналов SEO/SEM**
6. **Формирование "матрицы мониторинга"**

---

## 📊 TODO List

### ФАЗА 1: Расширение моделей данных (2-3 дня)

#### 1.1. Расширение модели Company для хранения соцсетей
- [ ] **Задача 1.1.1**: Создать миграцию для добавления полей соцсетей в таблицу `companies`
  - `facebook_url` VARCHAR(500)
  - `instagram_url` VARCHAR(500)
  - `twitter_url` VARCHAR(500) (или использовать существующий `twitter_handle`)
  - `linkedin_url` VARCHAR(500)
  - `youtube_url` VARCHAR(500)
  - `tiktok_url` VARCHAR(500)
  - Файл: `backend/alembic/versions/XXX_add_social_media_to_companies.py`

- [ ] **Задача 1.1.2**: Обновить модель `Company` в `backend/app/models/company.py`
  - Добавить поля для всех соцсетей
  - Добавить методы для извлечения handles из URL

- [ ] **Задача 1.1.3**: Обновить схемы Pydantic для API
  - `backend/app/schemas/company.py` (если существует)
  - Или создать новые схемы для onboarding

#### 1.2. Расширение SourceType для новых источников
- [ ] **Задача 1.2.1**: Добавить новые типы источников в `SourceType` enum
  - `FACEBOOK = "facebook"`
  - `INSTAGRAM = "instagram"`
  - `LINKEDIN = "linkedin"`
  - `YOUTUBE = "youtube"`
  - `TIKTOK = "tiktok"`
  - Файл: `backend/app/models/news.py`

- [ ] **Задача 1.2.2**: Создать миграцию для обновления enum `sourcetype` в PostgreSQL
  - Файл: `backend/alembic/versions/XXX_add_social_media_source_types.py`

- [ ] **Задача 1.2.3**: Обновить frontend типы
  - `frontend/src/types/index.ts` - добавить новые SourceType

#### 1.3. Создание модели для матрицы мониторинга
- [ ] **Задача 1.3.1**: Создать модель `CompetitorMonitoringMatrix`
  - Таблица: `competitor_monitoring_matrices`
  - Поля:
    - `company_id` UUID (FK to companies)
    - `monitoring_config` JSON (конфигурация мониторинга)
    - `social_media_sources` JSON (найденные соцсети)
    - `website_sources` JSON (найденные страницы сайта)
    - `news_sources` JSON (источники новостей)
    - `marketing_sources` JSON (маркетинговые страницы)
    - `seo_signals` JSON (SEO/SEM сигналы)
    - `last_updated` TIMESTAMP
  - Файл: `backend/app/models/competitor.py` (добавить класс)

- [ ] **Задача 1.3.2**: Создать миграцию для таблицы `competitor_monitoring_matrices`
  - Файл: `backend/alembic/versions/XXX_add_monitoring_matrix.py`

---

### ФАЗА 2: Сервисы извлечения данных (4-5 дней)

#### 2.1. Сервис поиска соцсетей
- [ ] **Задача 2.1.1**: Создать `SocialMediaExtractor` сервис
  - Файл: `backend/app/services/social_media_extractor.py`
  - Методы:
    - `extract_social_media_from_website(website_url: str) -> Dict[str, Optional[str]]`
    - `extract_from_meta_tags(soup: BeautifulSoup) -> Dict`
    - `extract_from_footer(soup: BeautifulSoup) -> Dict`
    - `extract_from_contact_page(soup: BeautifulSoup) -> Dict`
    - `normalize_social_url(url: str, platform: str) -> Optional[str]`

- [ ] **Задача 2.1.2**: Реализовать парсинг соцсетей из:
  - Meta tags (`og:site_name`, `twitter:site`)
  - Footer сайта (поиск ссылок на соцсети)
  - Страница "About" / "Contact"
  - JSON-LD structured data

- [ ] **Задача 2.1.3**: Добавить валидацию URL соцсетей
  - Проверка формата URL
  - Проверка доступности (опционально, с таймаутом)

#### 2.2. Сервис парсинга сайтов (структурные изменения)
- [ ] **Задача 2.2.1**: Создать `WebsiteStructureMonitor` сервис
  - Файл: `backend/app/services/website_structure_monitor.py`
  - Методы:
    - `capture_website_snapshot(website_url: str) -> Dict[str, Any]`
    - `detect_structure_changes(previous_snapshot: Dict, current_snapshot: Dict) -> Dict`
    - `extract_key_pages(website_url: str) -> List[str]` (главная, about, pricing, blog, news, careers)

- [ ] **Задача 2.2.2**: Реализовать извлечение структуры:
  - Навигационное меню (ссылки)
  - Основные страницы (pricing, features, about, blog, news, careers)
  - Метаданные страниц (title, description, keywords)
  - Структура HTML (основные секции)

- [ ] **Задача 2.2.3**: Реализовать сравнение снимков:
  - Diff по структуре навигации
  - Diff по основным страницам
  - Diff по метаданным

#### 2.3. Сервис сбора новостей и пресс-релизов
- [ ] **Задача 2.3.1**: Расширить существующий `UniversalBlogScraper`
  - Файл: `backend/app/scrapers/universal_scraper.py`
  - Добавить поддержку пресс-релизов:
    - Поиск страниц `/press`, `/press-releases`, `/newsroom`
    - Парсинг пресс-релизов с датами

- [ ] **Задача 2.3.2**: Создать специализированный `PressReleaseScraper`
  - Файл: `backend/app/scrapers/press_release_scraper.py`
  - Методы:
    - `find_press_release_page(website_url: str) -> Optional[str]`
    - `scrape_press_releases(press_url: str) -> List[Dict]`

- [ ] **Задача 2.3.3**: Интегрировать с существующей системой новостей
  - Использовать `NewsIngestionService` для сохранения
  - Категоризация как `PRESS_RELEASE`

#### 2.4. Сервис отслеживания маркетинговых изменений
- [ ] **Задача 2.4.1**: Создать `MarketingChangeDetector` сервис
  - Файл: `backend/app/services/marketing_change_detector.py`
  - Методы:
    - `detect_pricing_changes(company_id: UUID) -> List[Dict]` (использовать существующий `CompetitorChangeService`)
    - `detect_banner_changes(website_url: str) -> List[Dict]`
    - `detect_landing_page_changes(landing_url: str) -> List[Dict]`
    - `detect_new_products(product_page_url: str) -> List[Dict]`
    - `detect_job_postings(careers_url: str) -> List[Dict]`

- [ ] **Задача 2.4.2**: Реализовать детекцию изменений:
  - **Баннеры**: Сравнение изображений баннеров на главной странице (hash изображений)
  - **Лендинги**: Сравнение структуры и контента лендингов
  - **Новые продукты**: Парсинг страницы продуктов, сравнение списка
  - **Вакансии**: Парсинг страницы careers, сравнение списка вакансий

- [ ] **Задача 2.4.3**: Интегрировать с `CompetitorChangeEvent`
  - Создавать события изменений для маркетинговых изменений
  - Использовать `SourceType` для категоризации

#### 2.5. Сервис сбора SEO/SEM сигналов
- [ ] **Задача 2.5.1**: Создать `SEOSignalCollector` сервис
  - Файл: `backend/app/services/seo_signal_collector.py`
  - Методы:
    - `collect_seo_signals(website_url: str) -> Dict[str, Any]`
    - `extract_meta_tags(soup: BeautifulSoup) -> Dict`
    - `extract_structured_data(soup: BeautifulSoup) -> Dict`
    - `check_robots_txt(website_url: str) -> Dict`
    - `check_sitemap(website_url: str) -> List[str]`

- [ ] **Задача 2.5.2**: Реализовать сбор сигналов:
  - Meta tags (title, description, keywords, og:tags)
  - Structured data (JSON-LD, Schema.org)
  - Robots.txt анализ
  - Sitemap.xml анализ
  - Canonical URLs
  - Hreflang tags

- [ ] **Задача 2.5.3**: Добавить сравнение SEO сигналов
  - Diff по meta tags
  - Diff по structured data
  - Изменения в sitemap

---

### ФАЗА 3: Celery задачи для процессов наблюдения (3-4 дня)

#### 3.1. Задача поиска соцсетей
- [ ] **Задача 3.1.1**: Создать Celery задачу `discover_social_media`
  - Файл: `backend/app/tasks/observation.py` (новый файл)
  - Метод: `discover_social_media.delay(company_id: str)`
  - Использовать `SocialMediaExtractor`
  - Сохранять результаты в `Company` модель и `CompetitorMonitoringMatrix`

- [ ] **Задача 3.1.2**: Добавить обработку ошибок и ретраи
  - Retry логика для недоступных сайтов
  - Логирование результатов

#### 3.2. Задача парсинга сайтов
- [ ] **Задача 3.2.1**: Создать Celery задачу `capture_website_structure`
  - Файл: `backend/app/tasks/observation.py`
  - Метод: `capture_website_structure.delay(company_id: str)`
  - Использовать `WebsiteStructureMonitor`
  - Сохранять снимки в `CompetitorMonitoringMatrix`

- [ ] **Задача 3.2.2**: Реализовать периодическое сравнение
  - Задача `compare_website_structures.delay(company_id: str)`
  - Сравнение текущего снимка с предыдущим
  - Создание `CompetitorChangeEvent` при обнаружении изменений

#### 3.3. Задача сбора новостей
- [ ] **Задача 3.3.1**: Расширить существующую задачу `scrape_company_news`
  - Файл: `backend/app/tasks/scraping.py`
  - Добавить поддержку пресс-релизов
  - Использовать `PressReleaseScraper`

- [ ] **Задача 3.3.2**: Создать задачу `scrape_press_releases`
  - Файл: `backend/app/tasks/observation.py`
  - Метод: `scrape_press_releases.delay(company_id: str)`
  - Парсинг пресс-релизов и сохранение как новости

#### 3.4. Задача отслеживания маркетинга
- [ ] **Задача 3.4.1**: Создать Celery задачу `detect_marketing_changes`
  - Файл: `backend/app/tasks/observation.py`
  - Метод: `detect_marketing_changes.delay(company_id: str)`
  - Использовать `MarketingChangeDetector`
  - Создавать `CompetitorChangeEvent` для каждого изменения

- [ ] **Задача 3.4.2**: Реализовать подзадачи:
  - `detect_banner_changes.delay(company_id, website_url)`
  - `detect_landing_changes.delay(company_id, landing_url)`
  - `detect_new_products.delay(company_id, products_url)`
  - `detect_job_postings.delay(company_id, careers_url)`

#### 3.5. Задача сбора SEO сигналов
- [ ] **Задача 3.5.1**: Создать Celery задачу `collect_seo_signals`
  - Файл: `backend/app/tasks/observation.py`
  - Метод: `collect_seo_signals.delay(company_id: str)`
  - Использовать `SEOSignalCollector`
  - Сохранять в `CompetitorMonitoringMatrix`

- [ ] **Задача 3.5.2**: Реализовать периодическое сравнение SEO
  - Задача `compare_seo_signals.delay(company_id: str)`
  - Сравнение текущих сигналов с предыдущими
  - Создание событий при изменениях

#### 3.6. Главная задача настройки наблюдения
- [ ] **Задача 3.6.1**: Создать главную задачу `setup_competitor_observation`
  - Файл: `backend/app/tasks/observation.py`
  - Метод: `setup_competitor_observation.delay(session_token: str, company_ids: List[str])`
  - Координирует все подзадачи:
    1. `discover_social_media` для каждой компании
    2. `capture_website_structure` для каждой компании
    3. `scrape_press_releases` для каждой компании
    4. `detect_marketing_changes` для каждой компании
    5. `collect_seo_signals` для каждой компании
    6. `build_monitoring_matrix` (формирование матрицы)

- [ ] **Задача 3.6.2**: Реализовать прогресс-трекинг
  - Использовать Celery `current_task.update_state()`
  - Сохранять прогресс в `OnboardingSession.observation_config`
  - Возвращать процент выполнения

- [ ] **Задача 3.6.3**: Реализовать формирование матрицы мониторинга
  - Задача `build_monitoring_matrix.delay(company_id: str)`
  - Агрегирует все собранные данные
  - Сохраняет в `CompetitorMonitoringMatrix`

---

### ФАЗА 4: Интеграция с онбордингом (2-3 дня)

#### 4.1. Обновление API онбординга
- [ ] **Задача 4.1.1**: Обновить endpoint `/observation/setup`
  - Файл: `backend/app/api/v1/endpoints/onboarding.py`
  - Заменить симуляцию на реальный вызов `setup_competitor_observation.delay()`
  - Возвращать реальный `task_id` от Celery

- [ ] **Задача 4.1.2**: Обновить endpoint `/observation/status`
  - Файл: `backend/app/api/v1/endpoints/onboarding.py`
  - Использовать `AsyncResult(task_id)` для получения статуса
  - Возвращать реальный прогресс из `current_task.info`

- [ ] **Задача 4.1.3**: Добавить обработку ошибок
  - Обработка failed задач
  - Retry логика
  - Информативные сообщения об ошибках

#### 4.2. Обновление фронтенда
- [ ] **Задача 4.2.1**: Обновить `ObservationSetupStep.tsx`
  - Файл: `frontend/src/components/onboarding/ObservationSetupStep.tsx`
  - Улучшить отображение прогресса (показывать этапы)
  - Добавить детализацию: "Поиск соцсетей...", "Парсинг сайтов...", etc.

- [ ] **Задача 4.2.2**: Добавить обработку ошибок на фронтенде
  - Показывать понятные сообщения об ошибках
  - Кнопка "Повторить" при ошибках

---

### ФАЗА 5: Периодическое обновление наблюдения (2-3 дня)

#### 5.1. Планировщик периодических задач
- [ ] **Задача 5.1.1**: Добавить Celery Beat задачи
  - Файл: `backend/app/celery_app.py`
  - Задачи:
    - `compare_website_structures` - ежедневно
    - `detect_marketing_changes` - ежедневно
    - `compare_seo_signals` - еженедельно
    - `scrape_press_releases` - ежедневно

- [ ] **Задача 5.1.2**: Интегрировать с `CrawlScheduleService`
  - Использовать существующую систему расписаний
  - Настраивать частоту обновления для каждой компании

#### 5.2. Уведомления об изменениях
- [ ] **Задача 5.2.1**: Интегрировать с системой уведомлений
  - Использовать `CompetitorNotificationService`
  - Отправлять уведомления при обнаружении изменений
  - Категоризировать по типу изменения (маркетинг, структура, SEO)

---

### ФАЗА 6: Тестирование и документация (2-3 дня)

#### 6.1. Unit тесты
- [ ] **Задача 6.1.1**: Тесты для `SocialMediaExtractor`
  - Файл: `backend/tests/unit/services/test_social_media_extractor.py`

- [ ] **Задача 6.1.2**: Тесты для `WebsiteStructureMonitor`
  - Файл: `backend/tests/unit/services/test_website_structure_monitor.py`

- [ ] **Задача 6.1.3**: Тесты для `MarketingChangeDetector`
  - Файл: `backend/tests/unit/services/test_marketing_change_detector.py`

- [ ] **Задача 6.1.4**: Тесты для `SEOSignalCollector`
  - Файл: `backend/tests/unit/services/test_seo_signal_collector.py`

- [ ] **Задача 6.1.5**: Тесты для Celery задач
  - Файл: `backend/tests/unit/tasks/test_observation_tasks.py`

#### 6.2. Интеграционные тесты
- [ ] **Задача 6.2.1**: E2E тест онбординга с реальными задачами
  - Файл: `backend/tests/integration/api/test_onboarding_observation.py`
  - Проверка полного флоу от `/observation/setup` до завершения

#### 6.3. Документация
- [ ] **Задача 6.3.1**: Обновить README.md
  - Добавить описание процессов наблюдения
  - Описать новые поля в моделях

- [ ] **Задача 6.3.2**: Создать документацию по API
  - Описать новые endpoints (если нужны)
  - Описать структуру `CompetitorMonitoringMatrix`

---

## 📅 Временная оценка

- **Фаза 1**: 2-3 дня
- **Фаза 2**: 4-5 дней
- **Фаза 3**: 3-4 дня
- **Фаза 4**: 2-3 дня
- **Фаза 5**: 2-3 дня
- **Фаза 6**: 2-3 дня

**Итого: 15-21 рабочий день (3-4 недели)**

---

## 🔧 Технические детали

### Зависимости

- **Существующие**:
  - `httpx` - для HTTP запросов
  - `beautifulsoup4` - для парсинга HTML
  - `celery` - для фоновых задач
  - `sqlalchemy` - для работы с БД

- **Новые (опционально)**:
  - `selenium` / `playwright` - для динамических сайтов (если нужно)
  - `Pillow` - для сравнения изображений (hash баннеров)
  - `python-dateutil` - для парсинга дат

### Структура файлов

```
backend/
├── app/
│   ├── models/
│   │   ├── company.py (обновить)
│   │   └── competitor.py (добавить CompetitorMonitoringMatrix)
│   ├── services/
│   │   ├── social_media_extractor.py (новый)
│   │   ├── website_structure_monitor.py (новый)
│   │   ├── marketing_change_detector.py (новый)
│   │   └── seo_signal_collector.py (новый)
│   ├── scrapers/
│   │   └── press_release_scraper.py (новый)
│   ├── tasks/
│   │   └── observation.py (новый)
│   └── api/v1/endpoints/
│       └── onboarding.py (обновить)
├── alembic/versions/
│   ├── XXX_add_social_media_to_companies.py
│   ├── XXX_add_social_media_source_types.py
│   └── XXX_add_monitoring_matrix.py
└── tests/
    ├── unit/services/
    │   ├── test_social_media_extractor.py
    │   ├── test_website_structure_monitor.py
    │   ├── test_marketing_change_detector.py
    │   └── test_seo_signal_collector.py
    └── integration/api/
        └── test_onboarding_observation.py
```

---

## ✅ Критерии готовности

1. ✅ Все миграции применены
2. ✅ Все сервисы реализованы и протестированы
3. ✅ Все Celery задачи работают корректно
4. ✅ Онбординг использует реальные задачи вместо симуляции
5. ✅ Прогресс отображается корректно на фронтенде
6. ✅ Периодические задачи настроены и работают
7. ✅ Уведомления отправляются при обнаружении изменений
8. ✅ Документация обновлена

---

## 🚀 Следующие шаги

После утверждения плана:
1. Начать с Фазы 1 (модели данных)
2. Последовательно реализовать все фазы
3. Тестировать каждую фазу перед переходом к следующей
4. Обновлять TODO list по мере выполнения







