## Итерация 4 — Расширенная аналитика и экспорт

### Цель
- Предоставить глубинный анализ конкурентной активности, трендов и изменений, а также расширенные возможности экспорта и повторного использования отчётов.

### Основные задачи
- **Метрики и аналитика**: построить тренды по тональности и объёму новостей, сравнение тарифных уровней, оценку скорости инноваций; рассчитать комбинированный `impact_score`, объединяющий новости, изменения цен и новые функции; создать граф связей «событие → последствие».
- **Knowledge Graph**: связать сущности (новость, тариф, функция, команда, продукт) для выявления причинно-следственных связей и сценариев.
- **Фронтенд**: добавить вкладки «Постоянные метрики» и «Актуальные сигналы», таймлайны изменений, сценарии сравнения (A/B), сохранение пользовательских отчётов (пресетов фильтров и компаний), отображение `impact_score`.
- **Экспорт**: обновить JSON/PDF/CSV выгрузки, включить новые метрики, историю изменений, настройки уведомлений; предусмотреть версии экспортов и опцию shareable link.
- **API**: ввести версионирование (`v2`) и фича-флаги для постепенного развертывания; документировать схемы `impact_score`, пресетов и экспортов.
- **Риски и производительность**: оптимизировать расчёты метрик (кеширование, фоновое обновление), добавить нагрузочные тесты, следить за деградацией NLP-моделей.
- **Документация**: обновить `README` (карта сервисов, инструкции по экспорту, описания ключевых UI-блоков), подготовить гайд для аналитиков по работе с расширенными метриками.
- **Тестирование**: e2e сценарии экспорта, визуальные тесты графиков/таймлайнов, интеграционные тесты версионированных API, проверка сохранения/загрузки пользовательских отчётов.

### Критерии готовности
- Расширенные метрики и графы отображаются в UI и доступны через API `v2`.
- Пользовательские отчёты сохраняются и повторно применяются.
- Экспорты содержат полный набор новых данных в каждом формате.
- Производительность расчётов находится в допустимых пределах, есть фич-флаги для контроля релиза.
- Документация отражает новые метрики, экспорт и ответственность компонентов.

### Реализовано на 2025-11
- ✅ Новые модели и миграция аналитики (`company_analytics_snapshots`, `impact_components`, `analytics_graph_edges`, `user_report_presets`).
- ✅ Сервис `AnalyticsService` вычисляет `impact_score`, тренды, velocity и синхронизирует knowledge graph.
- ✅ Celery-задачи `recompute_company_analytics`, `recompute_all_analytics`, `sync_company_knowledge_graph` + расписание в `celery_app`.
- ✅ API `/api/v2/analytics/*` (снапшоты, запуск пересчёта, выдача графа, управление пресетами) с фич-флагом `ENABLE_ANALYTICS_V2`.
- ✅ Обновлён `README`: карта файлов, прогресс итерации, описание API и сервисов.
- ✅ Комбинированные дашборды и A/B сценарии:
  - Вкладка **Persistent Metrics** получила сводный график для нескольких компаний/пресетов (`frontend/src/components/MultiImpactTrendChart.tsx`) и таблицу сравнительных метрик (impact, news volume, innovation velocity, knowledge graph, change log).
  - Вкладка **Current Signals** дополнилась A/B виджетом: выбор двух субъектов (компания или пресет), side-by-side сравнение сигналов, knowledge graph edges и последних change events, быстрый импорт пресетов в сравнение.
  - `frontend/src/pages/CompetitorAnalysisPage.tsx` управляет состоянием `comparisonSubjects`, `comparisonData`, периодами агрегации и синхронизацией с пресетами.
- ✅ Расширенный экспорт v2 (`/api/v2/analytics/export`): backend собирает полный payload (метрики, series, knowledge graph, change log, notification settings, список пресетов), frontend генерирует JSON, обновлённый PDF и нормализованный CSV (zip).
- 🔄 В работе: Playwright e2e сценарии (пересчёт → сравнение → экспорт), нагрузочные тесты API (k6), автоматизация проверки экспортов в CI.

### Основные пользовательские сценарии сравнения

1. **Preset vs Preset (A/B pricing bundles)**  
   - *Пример:* `Enterprise vs Growth` (пресеты из `user_report_presets`).  
   - Вкладка **Persistent Metrics** показывает объединённый trend по impact score, таблицу сравнения (impact, trend delta, news volume, innovation velocity).  
   - На вкладке **Current Signals** аналитик выбирает пресеты в left/right селектоpах и видит:
     - Количество связей knowledge graph, агрегированные change events.
     - Топ-новости/сигналы, влияющие на текущий impact.
     - Delta impact и контекст (positive/negative coverage).

2. **Primary Company vs Immediate Competitors**  
   - *Пример:* выбранная компания + 2 конкурента из автоподборов.  
   - Комбинированный график помогает оценить, где компания отстаёт по impact/innovation velocity; таблица подсвечивает знания и change log.
   - A/B виджет позволяет сравнить основную компанию и любого конкурента без смены вкладок.

3. **Смешанный сценарий (Company vs Preset)**  
   - Используется, когда пресет агрегирует группу (например, "Top 5 cloud vendors").  
   - Позволяет сопоставить индивидуальную компанию с усреднённым пресетом по impact score, knowledge graph coverage и change log.

### API и структуры данных

- **POST `/api/v2/analytics/comparisons`** — принимает `ComparisonRequest` (список субъектов, период, окно и фильтры) и возвращает:
  - `subjects` — метаданные компаний/пресетов и их фильтры.
  - `metrics` — агрегированные показатели по каждому субъекту (impact, activity, velocity, top news, impact components).
  - `series` — мультисерийный таймлайн impact score.
  - `change_log`, `knowledge_graph` — сгруппированные списки событий/рёбер.
- **POST `/api/v2/analytics/export`** — возвращает `AnalyticsExportResponse` с подготовленной структурой для JSON/PDF/CSV (версия `2.0.0`, слот `notification_settings`, `presets`, `comparison`).
- Фронтенд использует `ApiService.buildAnalyticsExport` + `ApiService.exportAnalysis`, где CSV выгружается в zip с нормализованными таблицами:
  - `subjects.csv`, `metrics.csv`, `impact_components.csv`, `series.csv`, `top_news.csv`, `knowledge_graph.csv`, `change_log.csv`, `notification_settings.csv`, `presets.csv`.

### UX заметки

- Фильтры (source type/topic/sentiment/min priority) автоматически прокидываются в `ComparisonRequest` и влияют на обе вкладки.
- Контролы периода и окна (`30/60/90` дней) влияют на батч-запрос `/comparisons` — данные на UI обновляются без повторного запуска анализа.
- Добавление пресета в сравнение работает из A/B виджета: пресет догружается на бэкенде, появляется в комбинированном графике и доступен для выбора в селекторах.

