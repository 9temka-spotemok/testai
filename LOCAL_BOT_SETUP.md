# Настройка локального бота для разработки

## Токен бота для локальной разработки

**Бот:** @short_news_bot_test_bot  
**Токен:** `8110193525:AAHo3lu_o6FjM6seg031QD5y-F_jKnP-2po`

## Настройка

### 1. Создайте файл `.env` в корне проекта

Создайте файл `.env` в корне проекта (рядом с `docker-compose.yml`) со следующим содержимым:

```bash
# Telegram Bot Token (локальный бот для разработки с polling)
TELEGRAM_BOT_TOKEN=8110193525:AAHo3lu_o6FjM6seg031QD5y-F_jKnP-2po
```

### 2. Убедитесь, что `.env` в .gitignore

Файл `.env` уже добавлен в `.gitignore`, так что токен не попадёт в git.

### 3. Перезапустите Docker контейнеры

После создания `.env` файла перезапустите контейнеры:

```bash
docker-compose down
docker-compose up -d
```

## Как это работает

- **Локально (Docker):** Использует токен из `.env` → polling режим
- **Railway (прод):** Использует токен из переменных окружения Railway → webhook режим

Оба бота независимы и не конфликтуют друг с другом!

## Проверка

После запуска проверьте логи бота:

```bash
docker logs shot-news-telegram-bot
```

Должны увидеть:
```
✅ Bot token configured: 81101935...
🤖 Starting Telegram bot polling...
```

Ошибки 409 Conflict больше не должны появляться, так как локальный бот использует свой токен.

